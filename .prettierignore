artifacts/**
packages/db/generated/**
.scannerwork/**
.specify/**
.claude/worktrees/**
.claude/worktree-pool/**
apps/project-tracker/docs/metrics/.locks/**
apps/project-tracker/docs/metrics/backups/**
# ADR-067: the sync generator (lib/data-sync) writes these JSONs via
# JSON.stringify (multiline arrays); prettier reformats short arrays inline.
# When the two disagree, writeJsonFile's skip-if-unchanged byte-compare ALWAYS
# misses and rewrites every file on every sync — the ADR-066 write-cascade,
# resurrected by the formatter mismatch. Let the generator own this format so
# the linter stops fighting it. (Same rationale as the context docs below.)
# NOTE: docs/metrics/schemas/** are hand-authored and intentionally NOT listed.
apps/project-tracker/docs/metrics/sprint-*/**/*.json
apps/project-tracker/docs/metrics/_global/Sprint_plan.json
apps/project-tracker/docs/metrics/_global/task-registry.json
apps/project-tracker/docs/metrics/_global/dependency-graph.json
apps/project-tracker/docs/metrics/_global/spec-tracker.json
pnpm-lock.yaml
# Auto-generated by apps/project-tracker/scripts/generate-context.ts —
# the generator writes its own format (which doesn't match prettier's
# proseWrap=always 80-char wrap rules). Listing here so the linter
# doesn't fight the generator after every regen.
docs/SESSION_CONTEXT.md
docs/CURRENT_STATE_REPORT.md
