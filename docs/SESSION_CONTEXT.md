# Session Context Snapshot

_Manually updated: 2026-05-23 (CI cleanup session) • Branch: main_

> This file is normally auto-generated from
> `apps/project-tracker/docs/metrics/` by
> `npx tsx apps/project-tracker/scripts/generate-context.ts`. **Run that
> command at the start of your next session that has a warm dev environment
> (`pnpm install` already done)** to refresh from canonical metrics. This
> snapshot was patched by hand on 2026-05-23 to correct the stale
> `Branch: master` reference and reflect the CI baseline cleanup work that
> shipped since the last auto-regen.

## Where We Left Off

Active sprint: **Sprint 18** (sprint-18) — 51 task files under
`apps/project-tracker/docs/metrics/sprint-18/`. The autogenerated snapshot
from 2026-04-26 reported 10/60 complete (17%). Multi-sprint per-task progress
is in `apps/project-tracker/docs/metrics/_global/Sprint_plan.csv` (333 rows,
single source of truth) — re-derive with `pnpm run sync:all-inventory`.

## Recent CI/Infrastructure Work (2026-05-13 → 2026-05-23)

A multi-day stabilization landed across 20+ PRs. Main is now CI-green on all
required status checks.

**Repo-wide structural fixes:**

- `squash_merge_commit_message: BLANK` (no more long-body waivers needed for
  PR squash merges).
- `commit_msg_lint.py` now exempts bot authors (`*[bot]@users.noreply.github.com`).
- Multi-scope commit-header regex accepts `chore(ci)(deps):` style (Dependabot).
- Branch-name regex accepts
  `feat|fix|docs|refactor|test|chore|agent|ci|build|perf|style|revert`.
- `audit-runner` Python: `skipped` tier-1 gates no longer count as failed
  (was breaking every CI-config-only PR).
- vitest exclude pattern: `*.integration.test.ts` correctly excludes
  Redis/OTel/Playwright-cross-process tests from unit-test runs (Integration
  Tests CI job picks them up via its own pnpm test:integration).

**Workflows pruned:**

- `terraform-drift.yml` deleted (broken commit-back-to-protected-main design).
- `orphan-audit.yml` deleted (false-positive flood + missing Slack secret).

**Branch protection enforced:**

- 15 required status checks on `main` (Lint, Type Check, Unit Tests,
  Architecture, Integration, Build, Security Scan, Sprint Plan Validation,
  PR Validation, Quality Checks, Test Coverage, Build Preview, Deploy Preview,
  PR Checks Security Scan, PR Summary).

**Lighthouse easy wins** (PR #101 + manifest in this PR): explicit viewport,
themeColor metadata, PWA manifest, icon.svg, apple-icon.svg. Bundle-size +
TTI improvements remain tracked at issue #84.

## Active Tasks (IN_PROGRESS)

Empty per the 2026-04-26 snapshot. Likely-stale — re-derive after running
the regen script.

## Next Up (per 2026-04-26 snapshot — may be stale)

- **DOC-015** — Docs Integrity Reconciliation (regenerate + sync design-doc
  totals). _Partially addressed_: sitemap.md fixed to satisfy the
  route-total-consistency test (PR #97).
- **IFC-032** — PHASE-005: OpenTelemetry Monitoring
- **IFC-211** — Goal Settings RBAC

## Git Activity

- **Branch:** `main` (`master` was renamed via PR #58's rename agent on
  2026-05-01).
- **Recent merges (top 20):** Run `git log origin/main --oneline -20`.

## How to Refresh This File Properly

```bash
# Option A — local script (needs node_modules, fast)
pnpm install
npx tsx apps/project-tracker/scripts/generate-context.ts

# Option B — via project-tracker dev server (no install needed)
# (start the dev server first: pnpm --filter @intelliflow/project-tracker dev)
curl -X POST http://localhost:3002/api/context
```

Either option also regenerates `docs/CURRENT_STATE_REPORT.md` with the full
per-sprint breakdown.
