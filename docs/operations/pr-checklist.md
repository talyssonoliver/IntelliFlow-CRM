# PR Checklist

- Builds locally (where applicable)
- Tests updated or rationale provided
- No secrets committed
- `pnpm run validate:sprint-data -- --strict` passes when metrics are touched
- `pnpm run validate:sprint0 -- --strict` passes when governance/metrics/docs
  touched
