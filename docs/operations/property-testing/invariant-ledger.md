# Invariant Ledger — Property-Based & Race-Condition Testing

> Generated 2026-05-29 from `race-condition-findings.json` (Phase 0 audit).
> Companion to ADR-054, `PROPERTY_TESTING_AUDIT.md`, and
> `property-test-roadmap.md`. **Single source of truth for the invariants under
> test** — every property/concurrency test should reference an invariant id from
> this ledger.

This ledger enumerates **107 confirmed invariants** (across 11 bounded contexts)
plus **43 pure-domain property candidates**. 4 candidate races were dismissed as
already-guarded (see audit doc appendix). Severity: 6 Critical / 36 High / 40
Medium / 25 Low.

## Mandatory invariant archetypes

The brief mandates eight invariant archetypes. Each is mapped to the matching
confirmed finding id(s) in this CRM, or marked not-applicable with a reason.

| #   | Archetype                                                                      | Mapped finding ids                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Balance/quota never negative unless authorised                                 | `RACE-QUOTA-01` (High), `RACE-QUOTA-03` (High), `RACE-QUOTA-M1` (High), `RACE-WORKE-04` (High), `RACE-QUOTA-02` (Medium), `RACE-QUOTA-M2` (Medium), `RACE-QUOTA-M3` (Medium), `RACE-TEST--05` (Low), `RACE-WORKE-08` (Low)                                                                                                                                                                                                           |
| 2   | A booking slot cannot be confirmed twice for the same resource/time window     | `RACE-BOOKI-01` (Critical), `RACE-BOOKI-02` (Critical), `RACE-BOOKI-03` (High), `RACE-BOOKI-M1` (High), `RACE-BOOKI-M2` (High), `RACE-QUOTA-M1` (High), `RACE-BOOKI-04` (Medium), `RACE-BOOKI-M3` (Medium), `RACE-ROUTI-M3` (Medium), `RACE-WORKE-02` (Medium), `RACE-PURE-14` (Low), `RACE-WORKE-05` (Low)                                                                                                                          |
| 3   | A counter/capacity cannot be decremented below zero                            | `RACE-ROUTI-01` (Critical), `RACE-QUOTA-01` (High), `RACE-QUOTA-03` (High), `RACE-QUOTA-M1` (High), `RACE-ROUTI-02` (High), `RACE-ROUTI-M1` (High), `RACE-WORKE-04` (High), `RACE-ROUTI-05` (Medium)                                                                                                                                                                                                                                 |
| 4   | Duplicate webhook/event IDs must not duplicate business effects                | `RACE-ENTIT-03` (Critical), `RACE-AUDIT-M1` (High), `RACE-AUDIT-M2` (High), `RACE-DEDUP-03` (High), `RACE-WEBHO-01` (High), `RACE-WEBHO-02` (High), `RACE-WEBHO-M1` (High), `RACE-WEBHO-M3` (High), `RACE-WORKE-M3` (High), `RACE-AUDIT-07` (Medium), `RACE-DEDUP-M2` (Medium), `RACE-WEBHO-03` (Medium), `RACE-WEBHO-04` (Medium), `RACE-WEBHO-05` (Medium), `RACE-WEBHO-M2` (Medium), `RACE-TEST--03` (Low), `RACE-WEBHO-06` (Low) |
| 5   | Idempotency keys produce at-most-once side effects                             | `RACE-ENTIT-03` (Critical), `RACE-RBAC-M1` (Critical), `RACE-AUDIT-M1` (High), `RACE-AUDIT-M2` (High), `RACE-BOOKI-M1` (High), `RACE-DEDUP-03` (High), `RACE-WEBHO-01` (High), `RACE-WEBHO-M1` (High), `RACE-WORKE-M3` (High), `RACE-AUDIT-06` (Medium), `RACE-QUOTA-M2` (Medium), `RACE-WEBHO-03` (Medium), `RACE-WEBHO-05` (Medium), `RACE-WEBHO-M2` (Medium), `RACE-PURE-12` (Low)                                                |
| 6   | RBAC permissions derive only from assigned roles                               | `RACE-RBAC-M1` (Critical), `RACE-RBAC-01` (High), `RACE-RBAC-03` (High), `RACE-RBAC-06` (High), `RACE-RBAC-07` (High), `RACE-RBAC-M2` (High), `RACE-RBAC-04` (Medium), `RACE-RBAC-05` (Medium), `RACE-RBAC-M3` (Medium)                                                                                                                                                                                                              |
| 7   | Subscription/entitlement transitions stay valid after random command sequences | `RACE-ENTIT-03` (Critical), `RACE-ENTIT-02` (High), `RACE-ENTIT-04` (High), `RACE-ENTIT-05` (High), `RACE-ENTIT-M1` (High), `RACE-RBAC-01` (High), `RACE-RBAC-07` (High), `RACE-ENTIT-01` (Medium), `RACE-ENTIT-06` (Medium), `RACE-ENTIT-M2` (Medium), `RACE-ENTIT-M3` (Medium), `RACE-RBAC-05` (Medium), `RACE-WEBHO-04` (Medium), `RACE-PURE-07` (Low)                                                                            |
| 8   | Audit log records critical state changes exactly once                          | `RACE-AUDIT-01` (Critical), `RACE-ROUTI-01` (Critical), `RACE-AUDIT-02` (High), `RACE-AUDIT-05` (High), `RACE-AUDIT-M1` (High), `RACE-AUDIT-M2` (High), `RACE-AUDIT-03` (Medium), `RACE-AUDIT-04` (Medium), `RACE-AUDIT-06` (Medium), `RACE-AUDIT-07` (Medium), `RACE-AUDIT-M3` (Medium), `RACE-QUOTA-02` (Medium), `RACE-ROUTI-03` (Medium), `RACE-ROUTI-M3` (Medium)                                                               |

## Ledger by bounded context

### audit-log — Audit log — exactly-once recording of critical state changes

| ID              | Sev      | Aggregate / service                                          | Business rule (invariant)                                                                                                                                                                                                          | Invalid states                                                                                                                                                                                                                                                                                                                                                                                                                           | Generated inputs (arbitraries)                                                                                                                                       | Command sequence candidates                                                                                                                                                                                                                                                                    | Required DB constraints                                                                                                                                                                                                                          | Coverage today | Test type      | Tier     | Owner     | Batch |
| --------------- | -------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------- | -------- | --------- | ----- |
| `RACE-AUDIT-01` | Critical | packages/adapters/src/audit/DurableAuditLogAdapter.ts        | Every audit entry must chain to the preceding entry via previousHash so tampering is detectable; concurrent writes must not silently produce duplicate previousHash values.                                                        | Two concurrent logSecurityEvent calls both write previousHash = 'GENESIS' instead of one being the hash of the other; verifyLogIntegrity returns valid=false for all events after the first concurrent pair, silently destroying the compliance guarantee; logBatchEvents loop writes events 2..N with the same previousHash as event 1 because this.previousHash is only updated after the outer transaction commits                    | fc.tuple(fc.array(fc.record({ eventType: fc.string(), tenantId: fc.constant('t1') }), { minLength: 2, maxLength: 10 }), fc.scheduler())                              | concurrentLogSecurityEvent: [logSecurityEvent(e1), logSecurityEvent(e2)] in parallel — assert previousHashes form a strict chain; interleavedBatch: logBatchEvents([e1,e2]) concurrent with logSecurityEvent(e3) — assert no duplicate previousHash in DB                                      | Add a per-tenant sequence column or use a SELECT ... FOR UPDATE on the last hash row to serialize chain extension; alternatively store previousHash exclusively in DB and fetch it inside the transaction.                                       | unit-mocked    | scheduler-race | stress   | audit-log | 2     |
| `RACE-AUDIT-02` | High     | apps/api/src/security/audit/audit-logger.ts                  | Every authentication event must be recorded atomically in both the AuditLogEntry table and the SecurityEvent table; a crash or DB error between the two writes must not leave the logs in an inconsistent state.                   | Login success recorded in AuditLogEntry but SecurityEvent is absent — security dashboard misses the event; Login failure SecurityEvent written but AuditLogEntry absent — compliance audit trail is incomplete; Retry logic could duplicate the SecurityEvent without duplicating the AuditLogEntry, creating asymmetric counts                                                                                                          | fc.record({ tenantId: fc.uuidV4(), actorId: fc.uuidV4(), ipAddress: fc.ipV4() })                                                                                     | injectWriteErrorAfterAuditLogEntry: mock securityEvent.create to throw; assert AuditLogEntry row is rolled back or compensated; concurrentLoginEvents: fire 20 logLoginSuccess calls in parallel against a real DB; assert SecurityEvent count === AuditLogEntry count                         | Wrap both creates in a single prisma.$transaction; add a cross-table correlation ID column so post-hoc audits can detect mismatches.                                                                                                             | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-05` | High     | packages/adapters/src/audit/DurableAuditLogAdapter.ts        | The integrity verification path must be able to retrieve a persisted SecurityEvent and recompute its hash; if the query always misses, tampered logs are undetectable.                                                             | verifyLogIntegrity always returns { valid: false, reason: 'EVENT_NOT_FOUND' } for all real events, making tamper detection useless; If Prisma is used directly (not the abstraction), the query will throw PrismaClientValidationError crashing the integrity check endpoint                                                                                                                                                             | fc.uuidV4() representing arbitrary eventIds — property: verifyLogIntegrity(id) for any id that was just logged must return valid=true                                | roundtrip_verify: logSecurityEvent(e) -> verifyLogIntegrity(result.eventId) -> assert valid=true; tamper_detect: logSecurityEvent(e) -> direct DB UPDATE of integrityHash -> verifyLogIntegrity -> assert valid=false                                                                          | Add `eventId String @unique` to SecurityEvent model (and migration), or change verifyLogIntegrity to look up by `id` (cuid) and expose that id in AuditLogResult.                                                                                | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-M1` | High     | packages/adapters/src/events/OutboxEventBusAdapter.ts        | Each domain event must be published to the outbox exactly once; two concurrent calls with the same event (e.g. retry on network timeout) must not create duplicate DomainEvent rows.                                               | Two concurrent publish(event) calls for the same DomainEvent ID both pass the findFirst check and insert duplicate rows; both get dispatched to AuditEventHandler, producing doubled audit entries; Caller retries publish() after a network timeout before the first insert commits; both inserts succeed                                                                                                                               | fc.record({ eventType: fc.constantFrom('LeadCreated','ContactUpdated'), aggregateId: fc.uuidV4(), tenantId: fc.uuidV4() })                                           | concurrent_publish: call publish(event) twice in parallel with the same event; assert domainEvent row count === 1; retry_after_timeout: publish(event) once, simulate network timeout, publish(event) again; assert no duplicate DomainEvent rows                                              | Add a computed unique column or a partial unique index on a deterministic idempotency field derived from (eventType, aggregateId, aggregateType, occurredAt) outside the JSON blob; or move idempotencyKey to a top-level column with `@unique`. | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-M2` | High     | apps/api/src/security/audit/writer.ts                        | Each logical audit event must appear exactly once in the AuditLogEntry table; retrying a failed write with the same eventId must not produce a duplicate row.                                                                      | Caller retries logLoginSuccess after a timeout; two AuditLogEntry rows exist for the same login — compliance query returns doubled event counts; async flush() at audit-logger.ts:218-228 splices entries, write fails, unshifts them back, then flushes again — each entry in toWrite is written twice (no ON CONFLICT guard)                                                                                                           | fc.record({ tenantId: fc.uuidV4(), actorId: fc.uuidV4(), eventId: fc.uuidV4() }) — property: writeEntry called twice with same eventId produces exactly 1 row        | retry_write: call writeEntry(prisma, entry, config) twice with the same entry.eventId; assert auditLogEntry row count === 1; flush_retry: enqueue entry, flush fails, flush again; assert exactly one DB row per entry                                                                         | Add `@@unique([eventId])` to `AuditLogEntry` in schema.prisma, then use `prisma.auditLogEntry.upsert` or `createMany({ skipDuplicates: true })` in writeEntry.                                                                                   | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-03` | Medium   | apps/api/src/security/audit/index.ts                         | Every audit write must be scoped to the correct tenant so cross-tenant audit data is impossible; writing through an unscoped PrismaClient connection bypasses the tenant middleware applied to `ctx.prismaWithTenant`.             | Singleton initialized with tenant-A's prisma; tenant-B's audit writes go through tenant-A's connection pool, losing per-tenant RLS policies if they were applied at the adapter level; validateTenant returns true then tenant is deleted before auditLogEntry.create, causing FK violation that swallows the audit event silently (console.error only)                                                                                  | fc.tuple(fc.uuidV4(), fc.uuidV4()) representing two different tenantIds racing to initialize the singleton                                                           | race_initialization: call getAuditLogger(prismaA) and getAuditLogger(prismaB) concurrently; assert both writes use correct tenant context; validateThenDelete: validateTenant passes, then DELETE tenant row, then auditLogEntry.create — assert graceful error handling not data loss         | none additional — FK constraint already present; fix is architectural (pass prisma per-call not once to singleton)                                                                                                                               | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-04` | Medium   | apps/api/src/security/audit/audit-logger.ts                  | Each buffered audit entry must be written to the database exactly once; concurrent flush invocations must not duplicate entries.                                                                                                   | batchSize overflow flush starts, splices 100 entries, begins await; timer fires before await resolves, splices next 100 (or 0), returns; first flush fails, unshifts 100 entries back — but timer flush already completed some of them; Two simultaneous shutdown() calls each call flush(), both splice the same entries                                                                                                                | fc.array(fc.record({ action: fc.constantFrom('CREATE','UPDATE','DELETE'), resourceId: fc.uuidV4() }), { minLength: 101, maxLength: 300 })                            | concurrent_flush: enqueue 150 entries, trigger flush() twice simultaneously, assert exactly 150 DB writes; flush_during_shutdown: call log() 50 times then shutdown() concurrently, assert no duplicate rows                                                                                   | Adding a DB-level unique constraint on (eventId) in AuditLogEntry would turn duplicates into detectable errors rather than silent over-counting.                                                                                                 | unit-mocked    | scheduler-race | standard | audit-log | 2     |
| `RACE-AUDIT-06` | Medium   | packages/adapters/src/audit/DurableAuditLogAdapter.ts        | A batch audit write failure must either roll back the entire batch or precisely identify and re-queue the failed events; silently incrementing a failureCount while the enclosing $transaction continues is incorrect.             | 3 of 5 events fail DB write but transaction commits the other 2; caller sees failureCount=3 but has no event IDs to retry with; A cross-tenant event triggers CrossTenantViolationError mid-batch; earlier events in the same transaction are committed, later events are skipped — compliance record is incomplete                                                                                                                      | fc.array(fc.record({ eventType: fc.string(), tenantId: fc.constantFrom('t1','t2') }), { minLength: 2, maxLength: 10 }) with injected DB failures on specific indices | partialFailureBatch: events[2].tenantId != context.tenantId; assert result.failureCount=1 and the failed result contains the original eventId not a new UUID; allFailBatch: mock all tx.securityEvent.create to throw; assert transaction rolls back and no AuditLogEntry rows are created     | none additional — fix is behavioral: either abort the entire transaction on first failure (all-or-nothing semantics) or collect failures without swallowing and return original event IDs.                                                       | unit-mocked    | pure-property  | standard | audit-log | 2     |
| `RACE-AUDIT-07` | Medium   | packages/adapters/src/repositories/PrismaOutboxRepository.ts | Each domain event must be dispatched to the AuditEventHandler exactly once; concurrent restart while a batch is in-flight must not replay the same events.                                                                         | Worker fetches batch, begins dispatching, crashes mid-batch; on restart the events are still PENDING and are dispatched again, causing duplicate AuditLogEntry rows; Horizontal scale with two worker processes: the lock from the $queryRaw SELECT is released as soon as the result set is returned; a second worker starting within that window can pick up the same rows                                                             | fc.array(fc.record({ eventType: fc.string(), aggregateId: fc.uuidV4() }), { minLength: 1, maxLength: 20 })                                                           | restartDuringDispatch: fetch batch, simulate crash after dispatch of first event, restart worker, assert second event not double-processed; twoPollers: two PrismaOutboxRepository instances fetch concurrently, assert no overlapping event IDs in the two result sets                        | Add an atomic CTE or a separate UPDATE ... WHERE status='PENDING' RETURNING inside the same raw SQL transaction to flip status to PROCESSING atomically with the SELECT.                                                                         | unit-mocked    | db-concurrency | standard | audit-log | 2     |
| `RACE-AUDIT-M3` | Medium   | apps/api/src/security/audit/writer.ts                        | If a tenant is deleted concurrently, the audit write must either complete atomically within the tenant's lifetime or fail loudly; silently swallowing the FK violation to console.error means the audit event is permanently lost. | validateTenant returns true; tenant row deleted (CASCADE triggers); auditLogEntry.create throws FK violation; fire-and-forget caller logs to console and discards — audit event permanently lost; Two concurrent validateTenant calls for the same tenantId: first returns true (tenant exists), tenant is then deleted by admin, second call also returns true (cache/timing) — both callers attempt INSERT and both fail with FK error | fc.record({ tenantId: fc.uuidV4() }) — interleaving: validateTenant → DELETE tenant → auditLogEntry.create                                                           | toctou_tenant_delete: validateTenant passes, delete tenant row, call auditLogEntry.create — assert graceful handling not silent loss; fire_and_forget_swallow: wrap logAction in .catch(console.error), trigger FK violation — assert monitoring/alerting is triggered not just console output | none additional — FK already enforced; fix is to remove validateTenant pre-check entirely (rely on the FK for enforcement) and handle the FK error explicitly with re-throw or metric increment rather than fire-and-forget swallow              | unit-mocked    | db-concurrency | standard | audit-log | 2     |

### booking-scheduling — Booking / Scheduling — double-booking, slot confirm at-most-once

| ID              | Sev      | Aggregate / service                                                   | Business rule (invariant)                                                                                                                                                               | Invalid states                                                                                                                                                                                                                                                                                                             | Generated inputs (arbitraries)                                                                                                                                                                                                    | Command sequence candidates                                                                                                                                                                                                                                                                                                                                                                               | Required DB constraints                                                                                                                                                                                                                                                                                            | Coverage today | Test type      | Tier     | Owner              | Batch |
| --------------- | -------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------- | -------- | ------------------ | ----- |
| `RACE-BOOKI-01` | Critical | apps/api/src/modules/legal/appointments.router.ts                     | An appointment slot cannot be confirmed twice; confirm is only valid when the current status is SCHEDULED.                                                                              | status=CONFIRMED written twice by two concurrent requests, both reading status=SCHEDULED before either commits; AppointmentConfirmedEvent emitted twice, triggering duplicate ICS sends and duplicate reminder scheduling                                                                                                  | fc.record({ appointmentId: fc.uuidV4(), concurrentClients: fc.constant(2) }) — two scheduler tasks racing to confirm the same appointment ID                                                                                      | T1: findUnique(id) → reads SCHEDULED; T2: findUnique(id) → reads SCHEDULED; T1: update(id, {status: CONFIRMED}) → writes CONFIRMED; T2: update(id, {status: CONFIRMED}) → writes CONFIRMED (second confirm wins)                                                                                                                                                                                          | Atomic conditional update: update WHERE status='SCHEDULED' and check rows-affected=1; OR wrap findUnique+update in a $transaction with RepeatableRead/Serializable isolation; OR add a DB partial unique index that disallows a second CONFIRMED row for the same appointment.                                     | unit-mocked    | scheduler-race | stress   | booking-scheduling | 2     |
| `RACE-BOOKI-02` | Critical | packages/application/src/usecases/scheduling/ScheduleAppointment.ts   | Overlapping appointments for the same organiser/resource cannot both be confirmed — the conflict gate must be evaluated and the booking persisted atomically.                           | Two appointments with identical (organizerId, startTime, endTime) both reach SCHEDULED status; Two overlapping appointments (partial overlap) both confirmed for the same attendee, violating the no-overlap invariant                                                                                                     | fc.record({ organizerId: fc.uuidV4(), startTime: fc.date({ min: new Date('2025-01-01') }), durationMinutes: fc.integer({ min: 15, max: 120 }) }) — generate two overlapping slots for same organiser, fire concurrently           | T1: findForConflictCheck([orgId], {start, end}) → returns []; T2: findForConflictCheck([orgId], {start, end}) → returns []; T1: save(appointment1) → INSERT rows; T2: save(appointment2) → INSERT rows (duplicate slot)                                                                                                                                                                                   | PostgreSQL EXCLUSION CONSTRAINT on (tenantId, tsrange(startTime, endTime)) using the && (overlaps) operator, or at minimum @@unique([tenantId, organizerId, startTime]) to block identical-start duplicates; wrap SELECT+INSERT in a SERIALIZABLE transaction so T2's phantom-read causes a serialisation failure. | unit-mocked    | db-concurrency | stress   | booking-scheduling | 2     |
| `RACE-BOOKI-03` | High     | packages/application/src/usecases/scheduling/RescheduleAppointment.ts | A cancellation of an appointment must not be silently reversed by a concurrent reschedule that loaded the entity before the cancellation committed.                                     | Appointment status reverts from CANCELLED back to SCHEDULED/CONFIRMED after the reschedule upsert wins; cancelledAt and cancellationReason fields are cleared by the reschedule upsert because the in-memory entity loaded before the cancel does not carry those fields                                                   | fc.record({ appointmentId: fc.uuidV4(), newStartTime: fc.date() }) — fire reschedule and cancel concurrently on the same appointment ID with artificial delay inserted between load and save                                      | T_reschedule: forTenant(tenantId).findById(id) → loads SCHEDULED; T_cancel: forTenant(tenantId).findById(id) → loads SCHEDULED; T_cancel: appointment.cancel() → in-memory CANCELLED; T_cancel: save(appointment) → upsert writes CANCELLED; T_reschedule: appointment.reschedule() → in-memory still SCHEDULED (stale); T_reschedule: save(appointment) → upsert overwrites with SCHEDULED (lost cancel) | Optimistic concurrency: add a version (Int @default(0)) column to Appointment; update WHERE version=loadedVersion AND increment version; throw conflict if rows_affected=0. Alternatively wrap load+save in withTransactionOptions({ isolationLevel: 'RepeatableRead' }).                                          | unit-mocked    | db-concurrency | standard | booking-scheduling | 2     |
| `RACE-BOOKI-M1` | High     | apps/workers/events-worker/src/maintenance/scheduled-jobs.ts          | Each appointment must generate at most one reminder notification per 1-hour window; a user must not receive duplicate reminder pushes for the same appointment.                         | User receives two (or more) identical appointment reminder notifications within the same hour when two events-worker instances process the same appointment concurrently (e.g., rolling deploy, pod restart overlap); Notification table accumulates duplicate rows for the same (sourceId, sourceType, recipientId) tuple | fc.record({ appointmentId: fc.uuidV4(), workersCount: fc.constant(2) }) — two concurrent worker invocations firing checkAppointmentReminders for the same appointment window                                                      | W1: findFirst(sourceId=X, sourceType='appointment_reminder', createdAt>=T-1h) → null; W2: findFirst(sourceId=X, sourceType='appointment_reminder', createdAt>=T-1h) → null; W1: notification.create(...) → row inserted; W2: notification.create(...) → second row inserted (duplicate reminder sent)                                                                                                     | Add @@unique([sourceId, sourceType, recipientId]) to the Notification model to enforce idempotency at the DB level, and catch P2002 in the job to treat it as 'already sent'.                                                                                                                                      | none           | scheduler-race | standard | booking-scheduling | 2     |
| `RACE-BOOKI-M2` | High     | packages/application/src/usecases/scheduling/CompleteAppointment.ts   | A cancellation of an appointment must not be silently reversed by a concurrent completion that loaded the entity before the cancellation committed.                                     | Appointment status reverts from CANCELLED back to COMPLETED after the complete-use-case upsert wins; cancelledAt and cancellationReason fields are cleared by the complete upsert because the in-memory entity loaded before the cancel does not carry those fields                                                        | fc.record({ appointmentId: fc.uuidV4() }) — fire CompleteAppointmentUseCase and CancelAppointmentUseCase concurrently on the same appointmentId with artificial delay between load and save                                       | T_complete: forTenant(tenantId).findById(id) → loads CONFIRMED; T_cancel: forTenant(tenantId).findById(id) → loads CONFIRMED; T_cancel: appointment.cancel() → in-memory CANCELLED; T_cancel: save(appointment) → upsert writes CANCELLED; T_complete: appointment.complete() → in-memory still CONFIRMED (stale); T_complete: save(appointment) → upsert overwrites with COMPLETED (lost cancel)         | Add version column (Int @default(0)) to Appointment model; update WHERE id AND version=loadedVersion; throw conflict if rows_affected=0. Alternatively wrap load+save in withTransactionOptions({ isolationLevel: 'RepeatableRead' }).                                                                             | unit-mocked    | db-concurrency | standard | booking-scheduling | 2     |
| `RACE-BOOKI-04` | Medium   | packages/adapters/src/repositories/PrismaAppointmentRepository.ts     | Status transitions must respect the state machine; COMPLETED or CANCELLED appointments must not be overwritten by a batch status update.                                                | Appointment in CANCELLED state gets overwritten to CONFIRMED by a racing batch operation; completedAt / cancelledAt timestamps become stale or cleared because batchUpdateStatus only sets them for the target status, not for the current status being overwritten                                                        | fc.array(fc.uuidV4(), { minLength: 1, maxLength: 50 }) for appointment IDs; fc.constantFrom('CONFIRMED', 'COMPLETED', 'CANCELLED') for target status — verify that no terminal-state appointment is moved to a non-terminal state | T_cancel: CancelAppointmentUseCase writes CANCELLED to appointment A; T_batch: batchUpdateStatus([A, B, C], tenantId, 'CONFIRMED') runs concurrently, overwrites A back to CONFIRMED                                                                                                                                                                                                                      | Add status precondition to batchUpdateStatus WHERE clause: { status: { notIn: ['CANCELLED', 'COMPLETED'] } } to prevent overwriting terminal states.                                                                                                                                                               | unit-mocked    | pure-property  | standard | booking-scheduling | 2     |
| `RACE-BOOKI-M3` | Medium   | apps/api/src/modules/legal/appointments.router.ts                     | All appointment status transitions must be routed through domain use-cases so that the state-machine guard (Appointment.confirm()) and domain-event dispatch are consistently enforced. | AppointmentConfirmedEvent never dispatched through the domain event bus, so any event-driven side effects (ICS generation, reminder scheduling) are silently skipped; Future improvements to confirmation logic (e.g., adding capacity checks, pricing triggers) in the domain layer will not be applied to this path      | fc.record({ appointmentId: fc.uuidV4(), concurrentClients: fc.constant(2) }) — two concurrent confirm calls on the same appointment                                                                                               | T1: ctx.prismaWithTenant.appointment.findUnique → reads SCHEDULED; T2: ctx.prismaWithTenant.appointment.findUnique → reads SCHEDULED; T1: ctx.prismaWithTenant.appointment.update(CONFIRMED) → writes; T2: ctx.prismaWithTenant.appointment.update(CONFIRMED) → writes (no guard)                                                                                                                         | A ConfirmAppointmentUseCase wrapping confirm() in a withTransaction with WHERE status='SCHEDULED' conditional update eliminates both the missing-use-case gap and the RACE-BOOKI-01 race in one change.                                                                                                            | unit-mocked    | scheduler-race | smoke    | booking-scheduling | 2     |

### entitlement-modules — Entitlement / Tenant Modules — subscription-like state transitions

| ID              | Sev      | Aggregate / service                                                | Business rule (invariant)                                                                                                                                                                                                                                   | Invalid states                                                                                                                                                                                                                                                                                                                                 | Generated inputs (arbitraries)                                                                                                                                                                                           | Command sequence candidates                                                                                                                                                                                                                                                                                              | Required DB constraints                                                                                                                                                                                                | Coverage today | Test type      | Tier     | Owner               | Batch |
| --------------- | -------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | -------- | ------------------- | ----- |
| `RACE-ENTIT-03` | Critical | apps/api/src/modules/billing/billing.router.ts                     | A Stripe subscription webhook event must be processed exactly once; replays or retries must not trigger additional module-state mutations.                                                                                                                  | Two concurrent webhook retries both enter syncModulesToPlan, one reads partially-applied state from the other and returns an incomplete module list to the caller; A spoofed webhook with a downgrade plan tier disables modules the tenant paid for                                                                                           | fc.array(fc.constantFrom('customer.subscription.updated', 'customer.subscription.created'), { minLength: 2, maxLength: 5 }) with same tenantId and varying planTier to model retry storms                                | Stripe fires customer.subscription.updated; tRPC endpoint processes, starts syncModulesToPlan; Stripe retry fires same event before first sync completes; Second syncModulesToPlan starts concurrently — torn read as per RACE-ENTIT-01                                                                                  | Add a processed_stripe_events table with (event_id STRING @unique) and check before every sync. Implement stripe.webhooks.constructEvent() signature verification.                                                     | none           | model-based    | standard | entitlement-modules | 3     |
| `RACE-ENTIT-02` | High     | packages/adapters/src/repositories/PrismaTenantModuleRepository.ts | A module entitlement check must reflect a single committed snapshot: the plan tier and the per-tenant overrides must be read atomically so merged access decisions are consistent.                                                                          | Plan read = STARTER (old), overrides read = post-PROFESSIONAL-sync rows — result grants LEGAL without a PROFESSIONAL plan; Plan read = PROFESSIONAL (new), overrides read = pre-sync rows — LEGAL appears absent even though plan now includes it                                                                                              | fc.record({ tenantId: fc.uuid(), fromPlan: fc.constantFrom('STARTER','PROFESSIONAL'), toPlan: fc.constantFrom('PROFESSIONAL','ENTERPRISE') }) — schedule plan upgrade concurrent with getEnabledModules call             | T1: getTenantPlan(tenantId) reads STARTER; T2: syncModulesToPlan(tenantId, PROFESSIONAL) commits — Workspace.plan is still STARTER since updateSubscription never writes it; T1: findMany overrides reads the new PROFESSIONAL upserted rows; T1: merge grants LEGAL (from overrides) under STARTER plan — or vice versa | Wrap getTenantPlan + findMany overrides in a single $transaction with isolation REPEATABLE READ.                                                                                                                       | none           | db-concurrency | standard | entitlement-modules | 3     |
| `RACE-ENTIT-04` | High     | apps/api/src/modules/billing/billing.router.ts                     | Each user must have at most one Stripe customer record; concurrent calls to ensureCustomer must not create duplicate Stripe customers for the same user.                                                                                                    | Two Stripe customer records for the same userId; Stripe charges can be split across both; The second caller receives an unhandled P2002 exception surfaced as a 500 INTERNAL_SERVER_ERROR to the UI                                                                                                                                            | fc.scheduler() with two concurrent ensureCustomer calls for the same userId, both starting with stripeCustomerId = null                                                                                                  | T1: reads ctx.user.stripeCustomerId = null; T2: reads ctx.user.stripeCustomerId = null; T1: stripe.createCustomer() returns cus_A; T2: stripe.createCustomer() returns cus_B; T1: user.update({ stripeCustomerId: cus_A }) succeeds; T2: user.update({ stripeCustomerId: cus_B }) throws P2002                           | @unique on User.stripeCustomerId already exists and is load-bearing. Add upsert-style logic: attempt update with WHERE stripeCustomerId IS NULL using a conditional atomic update, or use a DB-level advisory lock.    | none           | scheduler-race | standard | entitlement-modules | 3     |
| `RACE-ENTIT-05` | High     | apps/api/src/modules/billing/billing.router.ts                     | When a tenant upgrades their subscription, the module set must be synced to the new plan tier, not the old one.                                                                                                                                             | Tenant upgrades STARTER→PROFESSIONAL via updateSubscription; Workspace.plan stays STARTER; sync re-enables only STARTER modules; LEGAL never becomes available until a webhook fires; If webhook never fires (Stripe misconfiguration, network drop), the tenant remains on STARTER module access indefinitely despite paying for PROFESSIONAL | fc.record({ fromPlan: fc.constantFrom('STARTER','PROFESSIONAL'), toPlan: fc.constantFrom('PROFESSIONAL','ENTERPRISE','CUSTOM'), priceId: fc.string() }) — assert getEnabledModules post-sync includes all toPlan modules | updateSubscription(priceId=price_professional_monthly); stripe.updateSubscription succeeds; getTenantPlan reads Workspace.plan = STARTER; syncModulesToPlan(tenantId, STARTER) — LEGAL never enabled                                                                                                                     | No new DB constraint needed. Workspace.plan must be updated transactionally alongside syncModulesToPlan in updateSubscription, or the sync must derive the plan from the Stripe priceId rather than re-reading the DB. | none           | pure-property  | smoke    | entitlement-modules | 3     |
| `RACE-ENTIT-M1` | High     | packages/adapters/src/repositories/PrismaTenantModuleRepository.ts | When a tenant downgrades to a lower plan tier, modules not included in the new tier must have their override rows set to enabled=false; a downgrade must not silently preserve access to modules the tenant no longer pays for.                             | Tenant downgrades ENTERPRISE→STARTER; LEGAL, ANALYTICS, AI_INTELLIGENCE override rows remain enabled=true; getEnabledModules returns all six modules indefinitely; Tenant cancels subscription; syncModulesToPlan(STARTER) called; all non-CORE_CRM enterprise rows still enabled; tenant retains full paid access                             | fc.record({ fromPlan: fc.constantFrom('ENTERPRISE','PROFESSIONAL'), toPlan: fc.constantFrom('STARTER','PROFESSIONAL') }) — assert that getEnabledModules post-sync returns ONLY modules included in toPlan               | syncModulesToPlan(tenantId, ENTERPRISE) — creates enabled=true rows for all 6 modules; syncModulesToPlan(tenantId, STARTER) — only upserts CORE_CRM as enabled=true; getEnabledModules(tenantId) — still returns [CORE_CRM, LEGAL, SUPPORT, AI_INTELLIGENCE, ANALYTICS, COMMERCE]                                        | No new constraint needed. syncModulesToPlan must also updateMany({ where: { tenantId, moduleId: { notIn: planModules } }, data: { enabled: false, disabledAt: now } }) within the same $transaction as the upserts.    | none           | pure-property  | standard | entitlement-modules | 3     |
| `RACE-ENTIT-01` | Medium   | packages/adapters/src/repositories/PrismaTenantModuleRepository.ts | After a plan sync all enabled-module reads for that tenant must reflect a consistent post-sync snapshot, never a partial mix of old and new module states.                                                                                                  | getEnabledModules returns [CORE_CRM, SUPPORT] when the plan being synced is PROFESSIONAL which should also include LEGAL and ANALYTICS; A concurrent admin disableModule for LEGAL commits between the sync's upserts and the final read, returning LEGAL as disabled even though syncModulesToPlan just re-enabled it                         | fc.record({ tenantId: fc.uuid(), plan: fc.constantFrom('STARTER','PROFESSIONAL','ENTERPRISE','CUSTOM') }) with fc.scheduler() to interleave syncModulesToPlan and disableModule                                          | T1: syncModulesToPlan(tenantId, PROFESSIONAL) — upserts LEGAL; T2: disableModule(tenantId, LEGAL) — commits between T1 upserts and T1 final read; T1: getEnabledModules() — reads LEGAL as disabled                                                                                                                      | Wrap all upserts in syncModulesToPlan in a single $transaction with isolation SERIALIZABLE or REPEATABLE READ; include the getEnabledModules read inside the same transaction.                                         | none           | db-concurrency | standard | entitlement-modules | 3     |
| `RACE-ENTIT-06` | Medium   | apps/api/src/modules/billing/billing.router.ts                     | After a module toggle or plan change, all API replicas must serve the updated entitlement state within the SLA; stale entitlement data must not persist beyond the cache TTL on any replica.                                                                | Replica A processes toggleModule(LEGAL, enabled=false), invalidates its local cache; Replica B still serves LEGAL as enabled from its 5-minute cached entry; User routed to Replica B gains unauthorized LEGAL module access for up to 5 minutes                                                                                               | fc.record({ replicaCount: fc.integer({ min: 2, max: 8 }), ttlMs: fc.constant(300000) }) — model cache-invalidation lag as a property                                                                                     | Admin calls toggleModule(LEGAL, false) — hits Replica A; invalidateBillingCache called on Replica A only; User request hits Replica B — cached entry returns LEGAL as enabled                                                                                                                                            | No DB constraint needed. Replace in-process Map with Redis-backed cache or move to Next.js Data Cache with revalidateTag (already in use in apps/web for MODULE_ACCESS).                                               | none           | pure-property  | smoke    | entitlement-modules | 3     |
| `RACE-ENTIT-M2` | Medium   | apps/api/src/modules/subscription/subscription.router.ts           | The module list returned by toggleModule must reflect the state immediately after the toggle is applied; a concurrent syncModulesToPlan must not cause the response to include or omit modules that contradict the just-applied toggle.                     | Admin disables LEGAL; a concurrent syncModulesToPlan(PROFESSIONAL) commits between the disableModule write and the trailing getEnabledModules read; response incorrectly shows LEGAL as enabled, misleading the admin                                                                                                                          | fc.scheduler() interleaving toggleModule(LEGAL, false) and syncModulesToPlan(tenantId, PROFESSIONAL) — assert the toggleModule response shows LEGAL as disabled                                                          | T1: disableModule(tenantId, LEGAL) — commits; T2: syncModulesToPlan(tenantId, PROFESSIONAL) — re-enables LEGAL — commits; T1: getEnabledModules() reads LEGAL as enabled — response contradicts the just-applied toggle                                                                                                  | No new DB constraint needed. Wrap the enableModule/disableModule call and the trailing getEnabledModules read inside a single $transaction to guarantee the read sees the just-written state.                          | none           | db-concurrency | standard | entitlement-modules | 3     |
| `RACE-ENTIT-M3` | Medium   | packages/adapters/src/repositories/PrismaTenantModuleRepository.ts | isModuleEnabled must return a decision that reflects a single consistent DB snapshot; a module that was just enabled by a concurrent syncModulesToPlan must not be falsely denied because the override row did not exist at the moment of the first lookup. | LEGAL has no override row; isModuleEnabled reads null; syncModulesToPlan creates override row enabled=true; getTenantPlan reads STARTER; isModuleEnabled returns false — access denied incorrectly; If isModuleEnabled is used as a server-side gate (not just a UI hint), this produces a false denial for a module the tenant just paid for  | fc.scheduler() with isModuleEnabled(tenantId, LEGAL) concurrent with syncModulesToPlan(tenantId, PROFESSIONAL) — assert the result is true after the sync completes                                                      | T1: findUnique(tenantId_moduleId) returns null (no override); T2: syncModulesToPlan(tenantId, PROFESSIONAL) inserts override row enabled=true; T1: getTenantPlan returns STARTER; MODULE_PLAN_MAP[STARTER].includes(LEGAL) = false; returns false                                                                        | No new DB constraint needed. Wrap findUnique + getTenantPlan fallback in a $transaction with RepeatableRead isolation, or consolidate into a single query that joins TenantModule and Workspace.plan.                  | none           | db-concurrency | standard | entitlement-modules | 3     |

### rbac-auth-session — RBAC / Auth / Session / MFA — permission derivation, concurrent role mutation

| ID             | Sev      | Aggregate / service                      | Business rule (invariant)                                                                                                                                     | Invalid states                                                                                                                                                                                                                                                                                                                                     | Generated inputs (arbitraries)                                                                                                                                                                                                    | Command sequence candidates                                                                                                                                                                                       | Required DB constraints                                                                                                                                                                                                      | Coverage today | Test type      | Tier     | Owner             | Batch |
| -------------- | -------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | -------- | ----------------- | ----- |
| `RACE-RBAC-M1` | Critical | apps/api/src/services/mfa.service.ts     | A backup code must be invalidated exactly once upon successful use; it must not be accepted on any subsequent attempt.                                        | User authenticates with backup code X; code X is removed from the in-memory object but DB still has code X; after process restart (or cache miss), code X is re-accepted; On a different Railway instance that never had the cache entry, code X is always accepted                                                                                | fc.array(fc.hexaString({ minLength: 10, maxLength: 10 }), { minLength: 8, maxLength: 8 }) for backup code set; use same code index twice across two mfaService instances with shared DB                                           | verifyChallenge(id, backupCode[0], settings) → success; clearCache(); getUserMfaSettings(userId) → DB fetch; verifyChallenge(id2, backupCode[0], freshSettings) → must fail but succeeds                          | No new DB constraint needed. Fix: call await saveUserMfaSettings({ ...userMfaSettings }) inside verifyChallenge after validateChallengeCode returns valid for method='backup'.                                               | unit-mocked    | scheduler-race | standard | rbac-auth-session | 2     |
| `RACE-RBAC-01` | High     | apps/api/src/services/session.service.ts | A user may never hold more than maxConcurrentSessions (default 3) valid sessions simultaneously.                                                              | User ends up with 4+ simultaneous sessions when only 3 are allowed; Wrong session revoked when two concurrent calls each pick the same 'oldest' session                                                                                                                                                                                            | fc.tuple(fc.uuid(), fc.uuid(), fc.uuid()) mapped to three concurrent createSession inputs for the same userId; fc.scheduler() to interleave the enforceSessionLimit read with the subsequent set                                  | createSession(userId) \|\| createSession(userId) \|\| createSession(userId) \|\| createSession(userId); createSession(userId) interleaved with revokeSession(existingId)                                          | No additional DB constraint can fix the in-memory race; fix requires a per-user async mutex or moving enforcement to the DB layer with SELECT FOR UPDATE or a transaction.                                                   | unit-mocked    | scheduler-race | standard | rbac-auth-session | 1     |
| `RACE-RBAC-03` | High     | apps/api/src/modules/auth/auth.router.ts | After regeneration, exactly one set of backup codes must be valid; old codes must be atomically invalidated.                                                  | User A and user B (same user, two browser tabs) each receive a different valid backup-code set; one set is immediately invalidated by the other's write; User receives plaintext codes that no longer match the hashed codes persisted to DB                                                                                                       | fc.array(fc.hexaString({ minLength: 10, maxLength: 10 }), { minLength: 8, maxLength: 8 }) for two concurrent backup-code arrays; fc.scheduler() for interleaving                                                                  | getBackupCodes(userId) \|\| getBackupCodes(userId); regenerateBackupCodes(userId, totpCode) \|\| regenerateBackupCodes(userId, totpCode)                                                                          | No new DB constraint needed; fix requires wrapping read-generate-write in a DB transaction with row-level lock (SELECT ... FOR UPDATE on UserMfaSettings) or an optimistic version field.                                    | unit-mocked    | db-concurrency | standard | rbac-auth-session | 1     |
| `RACE-RBAC-06` | High     | apps/api/src/security/rbac.ts            | A user's effective permission set must only include role assignments and overrides belonging to their current tenant.                                         | User who is ADMIN in tenant A but USER in tenant B receives ADMIN permissions when authenticating in tenant B context; A user permission override applied in tenant A (e.g., deny lead:delete) bleeds into tenant B reads                                                                                                                          | fc.record({ userId: fc.uuid(), tenantAId: fc.uuid(), tenantBId: fc.uuid(), roleInTenantA: fc.constant('ADMIN'), roleInTenantB: fc.constant('VIEWER') }) — assert getPermissionsWithDB scoped to tenantB never returns ADMIN perms | assignRole(userId, tenantA, ADMIN) then getPermissionsWithDB(userId, VIEWER) in context of tenantB                                                                                                                | Add tenantId to the WHERE clause in getUserRBACRoles and applyUserPermissionOverrides. Consider replacing @@unique([userId, roleId]) with @@unique([userId, roleId, tenantId]) to allow tenant-scoped role assignments.      | unit-mocked    | pure-property  | standard | rbac-auth-session | 1     |
| `RACE-RBAC-07` | High     | apps/api/src/services/mfa.service.ts     | getMfaStatus and isUserMfaEnabled must reflect the committed database state, not optimistically set in-process cache.                                         | User disables MFA; DB upsert fails silently; cache says MFA disabled; login bypasses MFA gate; User enables MFA on one tab; another concurrent request reads stale cache saying MFA is disabled and creates a session without MFA challenge                                                                                                        | fc.boolean() for totpEnabled; fc.boolean() for dbFailure; assert cache state equals DB state after saveUserMfaSettings completes (success or failure)                                                                             | saveUserMfaSettings({ totpEnabled: true }) with DB failure, then isUserMfaEnabled() — must return false; saveUserMfaSettings(disable) raced with login flow calling isUserMfaEnabled()                            | No new constraint needed. Fix: write to cache only after confirmed DB commit, or invalidate cache on any error.                                                                                                              | unit-mocked    | scheduler-race | smoke    | rbac-auth-session | 1     |
| `RACE-RBAC-M2` | High     | apps/api/src/services/session.service.ts | A user may never hold more than maxConcurrentSessions (default 3) valid sessions simultaneously, even after a process restart or on a new horizontal replica. | User has 3 sessions in DB from previous process run; new process starts (Railway deploy, crash-restart); user creates 3 more sessions without any revocation — ends with 6 sessions in DB; On a second Railway replica (no shared memory), the same user can create maxConcurrentSessions on each instance independently                           | fc.integer({ min: 1, max: 10 }) for existing DB session count; assert createSession enforces limit regardless of whether in-memory store is pre-populated                                                                         | seed DB with 3 sessions for userId; restart SessionService (clear module-level Maps); createSession(userId) x3 — expect 3 revocations in DB, not 6 total sessions                                                 | No DB constraint can fix this; fix requires enforceSessionLimit to query DB when sessionsByUser is missing: if (!userSessions) { await this.populateUserSessionsFromDb(userId); userSessions = sessionsByUser.get(userId); } | unit-mocked    | db-concurrency | standard | rbac-auth-session | 2     |
| `RACE-RBAC-04` | Medium   | apps/api/src/security/rbac.ts            | Role assignment must be atomic: if a revocation completes between the role-lookup and the upsert, the upsert must not re-create the assignment.               | Admin revokes a role but the assignment reappears because a concurrent assignRole re-upserted it after the delete; User briefly has elevated permissions after a revocation due to stale in-process cache (60s TTL)                                                                                                                                | fc.uuid() for userId; fc.constantFrom('ADMIN','MANAGER','SALES_REP') for roleName; two concurrent Promises: [assignRole(u, t, r), removeRole(u, r)]                                                                               | assignRole(userId, tenantId, roleName) raced with removeRole(userId, roleName); Two concurrent assignRole(userId, tenantId, roleName) calls (idempotency check)                                                   | Wrap findUnique + upsert/deleteMany in a Prisma $transaction([], { isolationLevel: 'Serializable' }) for mutation pairs.                                                                                                     | unit-mocked    | db-concurrency | standard | rbac-auth-session | 1     |
| `RACE-RBAC-05` | Medium   | apps/api/src/security/rbac.ts            | After a role is assigned or revoked, the effective permission set for the user must not be served from a stale cache beyond a bounded window.                 | User whose role was just revoked continues to pass can() checks for up to 60 seconds on the same or another process instance; User assigned a higher-privilege role does not gain access until cache expires                                                                                                                                       | fc.record({ userId: fc.uuid(), role: fc.constantFrom('ADMIN','MANAGER','SALES_REP','USER','VIEWER'), cacheAgeMs: fc.integer({ min: 0, max: 120000 }) }) to assert that permissions never come from cache older than cacheMaxAge   | assignRole(userId, ADMIN) followed immediately by getPermissionsWithDB(userId, VIEWER) — cache must be invalid; removeRole(userId, ADMIN) followed by can({ userId, userRole: 'ADMIN', ... }) — must return false | No DB constraint needed. Fix is infrastructure: replace in-process Map with Redis cache with PUBLISH/SUBSCRIBE invalidation, or reduce TTL to 0 for security-critical paths.                                                 | unit-mocked    | pure-property  | smoke    | rbac-auth-session | 1     |
| `RACE-RBAC-M3` | Medium   | apps/api/src/services/mfa.service.ts     | getMfaStatus and isUserMfaEnabled must not serve cache entries that have no TTL and can never expire organically.                                             | Support admin directly updates UserMfaSettings in DB to re-enable MFA for a compromised account; the process's mfaSettingsCache still shows totpEnabled: false and bypasses the MFA gate indefinitely; Process A disables MFA (updates its own cache + DB); Process B's cache still has totpEnabled: true and enforces MFA challenge unnecessarily | fc.boolean() for totpEnabled; fc.boolean() for outOfBandDbChange; assert isUserMfaEnabled() reflects DB state within a bounded window after any change                                                                            | getUserMfaSettings(userId) — populates cache; direct DB update to flip totpEnabled; getUserMfaSettings(userId) — returns stale cache; isUserMfaEnabled() returns wrong value                                      | No new DB constraint needed. Fix: add a TTL timestamp to cache entries; reject entries older than e.g. 30 seconds in getUserMfaSettings.                                                                                     | unit-mocked    | pure-property  | smoke    | rbac-auth-session | 2     |

### routing-assignment — Routing / Assignment — read-check-write lost update, double assignment

| ID              | Sev      | Aggregate / service                                  | Business rule (invariant)                                                                                                                                                                                        | Invalid states                                                                                                                                                                                                                                                                                                                                                                       | Generated inputs (arbitraries)                                                                                                                                                                                                                | Command sequence candidates                                                                                                                                                                                                                                                 | Required DB constraints                                                                                                                                                                                                                 | Coverage today | Test type      | Tier     | Owner              | Batch |
| --------------- | -------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | -------- | ------------------ | ----- |
| `RACE-ROUTI-01` | Critical | apps/api/src/modules/ticket/ticket-routing.router.ts | A ticket is assigned to exactly one agent after concurrent routing; each routing call increments the selected agent's capacity exactly once.                                                                     | ticket.assigneeId set to agent A, then immediately overwritten to agent B by second concurrent call; agentAvailability.currentCapacity incremented twice for the same ticket/agent pair; two RoutingAudit rows with same ticketId exist after a single autoRoute request pair                                                                                                        | fc.record({ ticketId: fc.uuid(), tenantId: fc.uuid(), category: fc.constantFrom('BILLING','TECHNICAL','GENERAL') }) — run two concurrent autoRoute calls with identical ticketId                                                              | T1: findFirst(ticket) -> T2: findFirst(ticket) -> T1: findMatchingRule -> T2: findMatchingRule -> T1: routeTicket($tx) -> T2: routeTicket($tx); T1: suggestAssignees -> T2: suggestAssignees [same agent[0] selected] -> T1: routeTicket commits -> T2: routeTicket commits | Add idempotency guard inside routeTicket transaction: re-read ticket.assigneeId at transaction start and throw if already assigned (unless forceReroute). Also add WHERE currentCapacity < maxCapacity to agentAvailability.updateMany. | unit-mocked    | scheduler-race | stress   | routing-assignment | 1     |
| `RACE-ROUTI-02` | High     | apps/api/src/services/LeadRoutingService.ts          | An agent's currentCapacity never exceeds maxCapacity after any number of concurrent routing decisions.                                                                                                           | agentAvailability.currentCapacity > agentAvailability.maxCapacity after two concurrent routing calls targeting the same agent; Agent continues to receive new assignments after being over-capacity because the filter uses stale in-transaction reads                                                                                                                               | fc.record({ agentId: fc.uuid(), tenantId: fc.uuid(), currentCapacity: fc.integer({ min: 0, max: 9 }), maxCapacity: fc.constant(10) }) — run 3 concurrent routeLead calls, verify currentCapacity <= maxCapacity                               | T1: getEligibleAgentsInTx (sees capacity=9 < 10) -> T2: getEligibleAgentsInTx (sees capacity=9 < 10) -> T1: updateMany(increment 1) -> T2: updateMany(increment 1) -> capacity=11                                                                                           | Add WHERE currentCapacity < maxCapacity to each agentAvailability.updateMany call, and check count===1; also add a DB-level CHECK constraint on AgentAvailability.                                                                      | unit-mocked    | db-concurrency | standard | routing-assignment | 1     |
| `RACE-ROUTI-M1` | High     | apps/api/src/modules/routing/routing.router.ts       | Every assignment of a lead to an agent — whether automatic or manual — increments that agent's currentCapacity so capacity-based routing remains accurate.                                                       | Agent receives N manual assignments; currentCapacity remains 0 (or pre-assignment value); Capacity-based routing perpetually over-selects manually-loaded agents                                                                                                                                                                                                                     | fc.record({ agentId: fc.uuid(), tenantId: fc.uuid(), manualAssignCount: fc.integer({ min: 1, max: 10 }) }) — run manualAssignCount assignLead calls, assert agentAvailability.currentCapacity === manualAssignCount                           | assignLead(leadId=L1, userId=A) -> assignLead(leadId=L2, userId=A) -> read agentAvailability(userId=A) -> assert currentCapacity == 2 (will fail: currentCapacity == 0)                                                                                                     | none — code fix only                                                                                                                                                                                                                    | unit-mocked    | db-concurrency | standard | routing-assignment | 2     |
| `RACE-ROUTI-03` | Medium   | apps/api/src/modules/account/account-reassign.ts     | The previousOwnerId recorded in the audit log and emitted in the notification matches the actual owner at the time of the write, not a stale pre-transaction snapshot.                                           | Audit log records previousOwnerId=A but actual previous owner was already B at time of write (because B had just been set by a racing reassign); Notification sent to wrong user pair (previous owner who already lost ownership is notified again)                                                                                                                                  | fc.record({ entityId: fc.uuid(), ownerA: fc.uuid(), ownerB: fc.uuid(), ownerC: fc.uuid() }) — T1 reassigns entity to ownerB, T2 concurrently reassigns to ownerC; assert audit.previousOwnerId matches actual DB ownerId at time of T2 commit | T1: findFirst(entity) -> T2: findFirst(entity) [both see ownerId=A] -> T1: $tx updateMany(ownerId=B) commits -> T2: $tx updateMany(ownerId=C) commits — T2 audit says previousOwnerId=A but actual was B                                                                    | none needed — fix is to move the ownerId capture inside the $transaction (read current ownerId inside tx, return it from tx as previousOwnerId)                                                                                         | unit-mocked    | scheduler-race | standard | routing-assignment | 1     |
| `RACE-ROUTI-04` | Medium   | apps/api/src/modules/routing/routing.router.ts       | A tenant can only reorder routing rules that belong to their own tenant; a rule transferred or deleted between the count check and the transaction cannot be mutated.                                            | Rule deleted between count check and transaction, causing $transaction to throw P2025 (record not found) — currently unhandled, will surface as 500; Rule transferred to different tenant between check and transaction — update proceeds on a rule the caller no longer owns                                                                                                        | fc.array(fc.uuid(), { minLength: 1, maxLength: 20 }) as ruleIds — simulate concurrent delete of one ruleId between count and transaction                                                                                                      | T1: count(ruleIds, tenantId) = N (PASS) -> T2: delete(ruleIds[0]) -> T1: $transaction update(ruleIds[0]) -> P2025 or wrong-tenant write                                                                                                                                     | none — fix is code: move the ownership count check inside the $transaction, or add tenantId to each individual update WHERE clause                                                                                                      | none           | scheduler-race | standard | routing-assignment | 1     |
| `RACE-ROUTI-05` | Medium   | apps/api/src/services/LeadRoutingService.ts          | When forceReroute reassigns an already-owned lead, the previous agent's capacity is decremented and the new agent's capacity is incremented exactly once; capacity does not drift after any number of reassigns. | agentA.currentCapacity remains inflated after its lead is force-rerouted to agentB — agentA appears over-loaded and may be excluded from future routing even though it has capacity; Both concurrent routeLead (ordinary) and forceReroute routing calls succeed on the same lead — lead.ownerId correct (last-write-wins) but agentAvailability incremented twice with no decrement | fc.record({ leadId: fc.uuid(), agentA: fc.uuid(), agentB: fc.uuid() }) — T1 runs ordinary routeLead (lead.ownerId=null), T2 runs forceReroute concurrently; assert each agent's currentCapacity changed by exactly +1 or -1 as expected       | T1: routeLead (no forceReroute, lead.ownerId=null) -> T2: routeLead (forceReroute=true, same lead) -> T1 commits (ownerId=A, A.capacity++) -> T2 commits (ownerId=B, B.capacity++, A.capacity NOT decremented)                                                              | none — fix is code: inside the forceReroute path, read the current ownerId inside the transaction and decrement that agent's currentCapacity before incrementing the new agent's                                                        | unit-mocked    | scheduler-race | standard | routing-assignment | 1     |
| `RACE-ROUTI-M2` | Medium   | apps/api/src/modules/routing/routing.router.ts       | A tenant can only toggle or delete routing rules that belong to their own tenant; a concurrent delete between the ownership check and the write must not result in an unhandled 500 or cross-tenant mutation.    | Concurrent delete of the same rule by two callers: second delete throws P2025, surfaces as unhandled 500; Rule deleted between findFirst and toggle update: P2025 unhandled 500 instead of NOT_FOUND                                                                                                                                                                                 | fc.record({ ruleId: fc.uuid(), tenantId: fc.uuid() }) — concurrent toggle and delete targeting the same ruleId                                                                                                                                | T1: findFirst(rule, tenantId=X) PASS -> T2: delete(rule) commits -> T1: update({ where: { id } }) -> P2025 unhandled; T1: findFirst(rule) PASS -> T2: findFirst(rule) PASS -> T1: delete commits -> T2: delete throws P2025                                                 | none — code fix only                                                                                                                                                                                                                    | none           | scheduler-race | standard | routing-assignment | 2     |
| `RACE-ROUTI-M3` | Medium   | apps/api/src/modules/account/account.router.ts       | Each audit entry in a bulk reassign records the actual owner at the time the individual row write committed, not a snapshot taken at loop-start.                                                                 | Audit rows for 50-account bulk reassign record previousOwnerId values that were already superseded by a concurrent single-item reassign that raced between the outer read and the tx commit; Notifications sent to the wrong previous-owner for rows where a race changed ownership in the gap                                                                                       | fc.array(fc.uuid(), { minLength: 5, maxLength: 20 }) as accountIds — two concurrent bulkReassign calls on overlapping subsets; assert each audit previousOwnerId matches the actual DB ownerId at commit time                                 | BulkCall-1 outer-read(acct[3]) sees ownerId=A -> BulkCall-2 tx(acct[3]) commits ownerId=C -> BulkCall-1 tx(acct[3]) commits ownerId=B, audit says previousOwnerId=A (actual was C)                                                                                          | none — code fix only                                                                                                                                                                                                                    | unit-mocked    | scheduler-race | standard | routing-assignment | 2     |

### dedupe-merge — Duplicate detection & Merge — concurrent create dup, lost merge

| ID              | Sev    | Aggregate / service                                                 | Business rule (invariant)                                                                                                                                                                   | Invalid states                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Generated inputs (arbitraries)                                                                                                                                                                                                                                                                                                                                                                                                                                  | Command sequence candidates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Required DB constraints                                                                                                                                                                                                                                | Coverage today | Test type      | Tier     | Owner        | Batch |
| --------------- | ------ | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------- | -------- | ------------ | ----- |
| `RACE-DEDUP-01` | High   | packages/application/src/services/ContactService.ts                 | At most one Contact (or Lead) per (tenantId, email) pair may exist; concurrent creates with the same email must not both succeed.                                                           | Two Contact rows with same (tenantId, email) — prevented by DB constraint but results in an opaque error instead of a controlled duplicate response; Caller receives PersistenceError and retries, creating another race window; Auto-merge is triggered by the second concurrent request seeing the first's newly created row, leading to an unintended merge of a freshly created contact                                                                                                                                       | fc.record({ tenantId: fc.uuid(), email: fc.emailAddress(), firstName: fc.string(), lastName: fc.string() }) used as concurrent input to two createContact calls                                                                                                                                                                                                                                                                                                 | T1: existsByEmail(email) → false; T2: existsByEmail(email) → false; T1: upsert(newId1); T2: upsert(newId2) → DB unique violation on T2; T1: checkForCreate → proceed; T2: checkForCreate → proceed; T1: createContact; T2: createContact → constraint error                                                                                                                                                                                                                                                                                   | @@unique([tenantId, email]) already present — no new constraint needed. Fix is to catch the P2002 Prisma error code and return a typed DuplicateEmailError from the repository.                                                                        | unit-mocked    | db-concurrency | standard | dedupe-merge | 2     |
| `RACE-DEDUP-03` | High   | apps/api/src/modules/inbound/inbound.router.ts                      | Each portal submission (identified by submissionId) must produce at most one Lead row; retried portal webhooks must be idempotent.                                                          | Two concurrent POST requests with same submissionId + same email both pass the findFirst check before either commits, then both call createLead — the second fails on unique constraint but the error path does a findFirst without the submissionId tag, potentially returning a different lead; Portal retries same submissionId with slightly different email (data normalisation variation) → two Lead rows with same logical submission                                                                                      | fc.record({ submissionId: fc.uuid(), email: fc.emailAddress(), tenantId: fc.constant('tenant-1') }) with two concurrent createLead calls                                                                                                                                                                                                                                                                                                                        | T1: findFirst(email+tag) → null; T2: findFirst(email+tag) → null; T1: createLead → ok; T2: createLead → constraint violation → fallback findFirst → returns T1's lead; T1: findFirst → null; T2: findFirst → null; T1: createLead; T2: createLead(same submissionId, different email) → second lead row created, no constraint blocks it                                                                                                                                                                                                      | Add @@unique([tenantId, submissionId tag]) — not directly expressible in Prisma for array columns. Alternative: introduce a LeadSubmission table with @@unique([tenantId, submissionId]) and use connectOrCreate/createOrUpdate atomically.            | none           | scheduler-race | standard | dedupe-merge | 2     |
| `RACE-DEDUP-05` | High   | packages/adapters/src/repositories/PrismaContactRepository.ts       | Email uniqueness is tenant-scoped; a contact email valid in tenant B must not be blocked by an existing contact with the same email in tenant A.                                            | Tenant B cannot create a contact with email X because tenant A already has a contact with email X — blocked by existsByEmail returning true across tenants; updateContactEmail for tenant B's contact falsely detects a conflict against tenant A's contact with the same email                                                                                                                                                                                                                                                   | fc.tuple(fc.uuid(), fc.uuid()).filter(([a,b]) => a !== b) as (tenantA, tenantB) paired with fc.emailAddress() to prove the same email can be created in both tenants                                                                                                                                                                                                                                                                                            | Tenant A creates contact(email='x@y.com', tenantId='A'); Tenant B calls existsByEmail('x@y.com') → true; Tenant B's create is rejected                                                                                                                                                                                                                                                                                                                                                                                                        | No new DB constraint needed — @@unique([tenantId, email]) already correct. Fix: add tenantId parameter to ContactRepository.existsByEmail and findByEmail ports, pass it through from ContactService.                                                  | unit-mocked    | pure-property  | standard | dedupe-merge | 2     |
| `RACE-DEDUP-M1` | High   | packages/application/src/services/LeadService.ts                    | Lead conversion must atomically create the contact and mark the lead CONVERTED; a partial write (contact created, lead status not updated) must never persist.                              | Contact row created but lead status still QUALIFIED (contactRepository.save succeeded, leadRepository.save failed) — second convert call proceeds and creates a second Contact for the same lead; Two Contact rows with same leadId (Contact.leadId is @unique at schema.prisma:629 — this constraint will catch the second insert and throw a P2002, but only after the first duplicate contact is already committed); Lead event (LeadConvertedEvent) published but lead status in DB is QUALIFIED — downstream systems diverge | fc.record({ leadId: fc.uuid(), tenantId: fc.constant('t1') }) with two concurrent convertLead calls scheduled to interleave after the first contactRepository.save                                                                                                                                                                                                                                                                                              | T1: findById(leadId) → QUALIFIED; T2: findById(leadId) → QUALIFIED; T1: contact.save → ok; T1: lead.save → network timeout; T2: contact.save → P2002 (Contact.leadId @unique) → PersistenceError; lead still QUALIFIED in DB; T1: findById → QUALIFIED; T1: contact.save; T1: lead.save → ok; T2: findById → CONVERTED (now visible); T2: returns LeadAlreadyConvertedError — safe path; race only materializes if T2's read is before T1's lead.save commits                                                                                 | packages/db/prisma/schema.prisma:629 Contact.leadId @unique already prevents two Contacts for the same Lead. The real fix is wrapping contactRepository.save + leadRepository.save in a single $transaction in LeadService.convertLead.                | unit-mocked    | db-concurrency | standard |              |       |
| `RACE-DEDUP-M3` | High   | packages/application/src/services/ContactService.ts                 | An email update must atomically verify the new email is not in use within the same tenant and persist the change; a cross-tenant false conflict must not block a legitimate update.         | Tenant B's contact update is rejected because tenant A has the same email — false conflict from cross-tenant findByEmail; Two concurrent same-tenant contacts racing to claim the same email both pass the check; one gets P2002 PersistenceError instead of a typed DuplicateEmailError                                                                                                                                                                                                                                          | fc.tuple(fc.uuid(), fc.uuid()).filter(([a,b]) => a!==b) as (tenantA, tenantB) with fc.emailAddress() — tenantA's contact has email X; tenantB's contact tries to update to X                                                                                                                                                                                                                                                                                    | TenantA has contact(email='x@y.com'); TenantB calls updateContactEmail(contactId, 'x@y.com'); findByEmail('x@y.com') returns TenantA's contact; id !== contactId → reject: false positive; T1: findByEmail('x@y.com') → null; T2: findByEmail('x@y.com') → null; T1: save → ok; T2: save → P2002 → PersistenceError (opaque)                                                                                                                                                                                                                  | packages/db/prisma/schema.prisma:643 @@unique([tenantId, email]) already correct. Fix: add tenantId to findByEmail signature (aligns with RACE-DEDUP-05 fix), and catch P2002 in PrismaContactRepository.save() to return a typed DuplicateEmailError. | unit-mocked    | db-concurrency | standard |              |       |
| `RACE-DEDUP-02` | Medium | packages/application/src/services/ContactService.ts                 | A contact merge is irreversible; concurrent merge requests for the same pair in opposite directions must not both proceed past the pre-flight read.                                         | Both A->B and B->A merges enter mergeInTransaction concurrently; first commits, second reads stale state under READ COMMITTED and may attempt re-parenting of already-re-parented child rows before detecting the missing secondary; Duplicate ContactMergedEvent published (post-commit fire-and-forget not idempotent)                                                                                                                                                                                                          | fc.tuple(fc.uuid(), fc.uuid()) as (primaryId, secondaryId) with a scheduler that interleaves two concurrent calls: merge(A,B) and merge(B,A)                                                                                                                                                                                                                                                                                                                    | T1: findById(A) ok, findById(B) ok; T2: findById(B) ok, findById(A) ok; T1: $tx[findFirst(A,B), reparent, update(A), delete(B)]; T2: $tx[findFirst(B) missing → CrossTenantOrNotFoundError → rollback]; T1 and T2 both enter tx; T1 deletes B; T2 tries reparent on already-reparented children → noop but emits duplicate event                                                                                                                                                                                                              | No new constraint needed. Fix: set $transaction({ isolationLevel: 'Serializable' }) on mergeInTransaction, or add an optimistic-lock version column to Contact.                                                                                        | unit-mocked    | scheduler-race | standard | dedupe-merge | 2     |
| `RACE-DEDUP-04` | Medium | apps/api/src/modules/contact/contact.router.ts                      | When autoMergeOnExactEmail is enabled, the newly created contact must be merged into the primary before any other operation can reference it.                                               | New contact row is visible to other concurrent requests (e.g. a webhook assigning a task to contactId) before the merge deletes it, creating dangling FK references if the merge then succeeds; applyAutoMerge is swallowed (catch at line 620) and the un-merged duplicate persists indefinitely                                                                                                                                                                                                                                 | fc.record({ email: fc.emailAddress(), tenantId: fc.constant('t1') }) with a scheduler interleaving the create-commit and applyAutoMerge call                                                                                                                                                                                                                                                                                                                    | checkForCreate → auto-merge; createContact → commits newId; [concurrent webhook assigns task to newId]; applyAutoMerge → merges newId into primaryId; task now references deleted newId; checkForCreate → auto-merge; createContact → commits newId; applyAutoMerge throws (no dep wired) → caught/swallowed → duplicate persists                                                                                                                                                                                                             | No new constraint needed. Fix: make auto-merge failure non-swallowable (throw TRPCError) or atomically prevent the create+merge split by checking the existing contact before any DB write and returning the existing ID directly.                     | unit-mocked    | scheduler-race | smoke    | dedupe-merge | 2     |
| `RACE-DEDUP-06` | Medium | apps/api/src/modules/account/account-duplicate-detection.service.ts | Duplicate-detection for accounts must not allow two concurrent account-create requests with the same name/website to both succeed undetected.                                               | Two account rows with same (tenantId, name, website) created concurrently; no flag notification emitted for one because both concurrent checks saw no candidates; Auto-link by domain (linkContactsByDomain) later links contacts to the wrong account if there are two accounts with the same domain                                                                                                                                                                                                                             | fc.record({ tenantId: fc.constant('t1'), name: fc.string({ minLength: 3 }), website: fc.webUrl() }) as concurrent account create payloads                                                                                                                                                                                                                                                                                                                       | T1: checkForCreate(name='Acme') → proceed (no candidates yet); T2: checkForCreate(name='Acme') → proceed (no candidates yet); T1: accountService.createAccount → ok; T2: accountService.createAccount → ok (no constraint); two Acme accounts exist; T1: check → flag; T2: check → proceed (T1's row not committed yet); T2 creates second Acme silently                                                                                                                                                                                      | If hard dedup is desired: add @@unique([tenantId, name]) on Account. If advisory-only is intentional: add an async dedup job that reconciles after-the-fact or accept the documented behavior.                                                         | unit-mocked    | db-concurrency | standard | dedupe-merge | 2     |
| `RACE-DEDUP-M2` | Medium | apps/workers/events-worker/src/outbox/pollOutbox.ts                 | Each domain event occurrence must be written to the outbox exactly once; duplicate outbox rows for the same (aggregateId, eventType, mergedAt) must not cause duplicate downstream effects. | Two DomainEvent rows for ContactMerged on the same (primaryId, secondaryId) — downstream consumers (email notifications, analytics, audit log) fire twice; Webhook relay sends two 'contact.merged' notifications to the customer's system                                                                                                                                                                                                                                                                                        | Two concurrent merge(A,B) calls scheduled to both reach ContactService.ts:534 before either's event is flushed                                                                                                                                                                                                                                                                                                                                                  | T1: mergeInTransaction → ok → publish ContactMergedEvent; T2: mergeInTransaction → CrossTenantOrNotFoundError (B already deleted) → no event (safe path); T1 and T2 both reach mergeInTransaction concurrently under READ COMMITTED; T1 commits first deleting B; T2 reads B as null inside its tx; T2 throws before returning from mergeInTransaction; T2's post-commit block is never reached — safe under current code. Risk materializes only if a future refactor moves event publish inside the $transaction or if T2 somehow succeeds. | Add @@unique([aggregateId, eventType, occurredAt]) or an idempotency_key column with @@unique on DomainEvent to prevent duplicate outbox rows.                                                                                                         | unit-mocked    | db-concurrency | standard |              |       |
| `RACE-DEDUP-07` | Low    | apps/api/src/shared/duplicate-rule-evaluator.ts                     | evaluateDuplicateRules must return score in [0,100], never match an entity against itself, and be deterministic (same inputs always produce same output).                                   | threshold=0 with exact strategy silently uses floor=100 (resolveFloor:166) — a duplicate with score=50 is not flagged when caller intended threshold=0 to mean 'match everything'; levenshtein on emoji inputs (e.g. '🙂' vs '🙃') produces incorrect similarity scores due to JS surrogate-pair length; Self-match guard (inputId === candidate.id) at line 178 is bypassed when input has no id field                                                                                                                           | fc.record({ input: fc.record({ id: fc.option(fc.uuid()), email: fc.string(), phone: fc.string() }), candidates: fc.array(fc.record({ id: fc.uuid(), email: fc.string() }), { minLength: 1, maxLength: 20 }), rules: fc.array(fc.record({ field: fc.constantFrom('email','phone','name'), matchStrategy: fc.constantFrom('exact','normalized','fuzzy'), threshold: fc.integer({ min: 0, max: 100 }), isActive: fc.constant(true), sortOrder: fc.integer() })) }) | Invariant: score in [0,100] for all outputs; Invariant: no match where candidate.id === input.id; Invariant: evaluateDuplicateRules(x, [], rules).length === 0; Invariant: result is deterministic — calling twice with same args returns same result                                                                                                                                                                                                                                                                                         | None — pure function, no DB interaction.                                                                                                                                                                                                               | unit-mocked    | pure-property  | smoke    | dedupe-merge | 2     |

### pure-domain — Pure domain invariants — value objects, status machines, normalization, round-trips

| ID             | Sev    | Aggregate / service                                   | Business rule (invariant)                                                                                                                                                                                                  | Invalid states                                                                                                                                                                                                                                                                                                                          | Generated inputs (arbitraries)                                                                                                                                                                                                                                                                  | Command sequence candidates                                                                                                                                                                                                                                                                               | Required DB constraints                                                     | Coverage today | Test type     | Tier     | Owner       | Batch |
| -------------- | ------ | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------- | ------------- | -------- | ----------- | ----- |
| `RACE-PURE-04` | High   | packages/domain/src/crm/billing/Invoice.ts            | After any addLineItem or removeLineItem call, Invoice.totalAmount must equal sum(lineItem.total) + taxRate×subtotal, and amountDue must equal totalAmount - amountPaid + amountRefunded.                                   | After removeLineItem(0), totalAmount still equals old value if recalculate silently fails; amountDue < 0 is impossible because subtract fails, but amountDue could be stale/too-high after a partial failure                                                                                                                            | fc.array(fc.record({description:fc.string({minLength:1}),quantity:fc.integer({min:1,max:100}),unitPriceCents:fc.integer({min:0,max:1_000_000}),type:fc.constantFrom('SUBSCRIPTION','ONE_TIME','USAGE','CREDIT','DISCOUNT')}),{minLength:1,maxLength:10}); fc.float({min:0,max:100}) for taxRate | Invoice.create(props) → addLineItem(item) → assert totalAmount.cents === expected_sum_with_tax; Invoice.create(props) → removeLineItem(0) → assert totalAmount.cents reflects remaining items; create → addLineItem → removeLineItem(same index) — assert idempotency: totalAmount returns to original    | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-06` | High   | packages/domain/src/crm/billing/Invoice.ts            | At all times: amountDue.cents + amountPaid.cents - amountRefunded.cents === totalAmount.cents.                                                                                                                             | amountPaid + amountDue - amountRefunded !== totalAmount (accounting invariant broken); paymentStatus === 'PAID' while amountDue.cents > 0                                                                                                                                                                                               | fc.array(fc.oneof(fc.record({type:fc.constant('pay'),amount:fc.integer({min:1,max:1000})}), fc.record({type:fc.constant('refund'),amount:fc.integer({min:1,max:1000}),reason:fc.constantFrom('CUSTOMER_REQUEST','BILLING_ERROR')})),{minLength:1,maxLength:20})                                 | create → issue → sequence of recordPayment/processRefund — assert invariant after each step; create → issue → recordPayment(full) → processRefund(full) → assert status=OPEN, amountDue=totalAmount; create → issue → recordPayment(partial) → recordPayment(remainder) → assert status=PAID, amountDue=0 | none — pure domain invariant                                                | unit-mocked    | model-based   | standard | pure-domain | 1     |
| `RACE-PURE-01` | Medium | packages/domain/src/shared/Money.ts                   | Every Money instance must carry a valid, recognised ISO-4217 currency code that survives a create→toValue→fromCents round-trip unchanged.                                                                                  | Money.create(10, 'USD').isFailure === true (USD silently unsupported); SUPPORTED_CURRENCIES.filter(c => c==='GBP').length === 2 (duplicate entry wastes array slot)                                                                                                                                                                     | fc.constantFrom('GBP','EUR','CAD','AUD','JPY') for valid currencies; fc.string({minLength:3,maxLength:3}) for arbitrary strings to exercise rejection; fc.float({min:0,max:1e6}) for amounts                                                                                                    | Money.create(amount, currency) → toValue() → fromCents(cents, currency) — assert cents and currency preserved; Money.zero(currency).add(Money.create(x, currency)) — assert result equals Money.create(x, currency); Money.fromCents(n, c1).add(Money.fromCents(m, c2)) — assert failure iff c1 !== c2    | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-02` | Medium | packages/domain/src/shared/Money.ts                   | Money addition must be commutative (a+b === b+a in cents) and associative ((a+b)+c === a+(b+c) in cents); multiply must not produce fractional cents silently.                                                             | Money.create(0.001, 'GBP').cents === 0 (sub-cent amounts silently round to zero); chain of multiply(1/3).multiply(3) may not recover original cents                                                                                                                                                                                     | fc.integer({min:0,max:10_000_000}) for cents-based construction; fc.float({min:0,max:100_000,noNaN:true,noDefaultInfinity:true}) for decimal construction; fc.constantFrom('GBP','EUR','CAD','AUD','JPY')                                                                                       | a.add(b).cents === b.add(a).cents for all valid same-currency pairs; (a.add(b)).add(c).cents === a.add(b.add(c)).cents; Money.fromCents(n,c).toValue().cents === n; Money.create(x,c).cents === Math.round(x\*100) for non-zero-decimal currencies                                                        | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-03` | Medium | packages/domain/src/crm/opportunity/Opportunity.ts    | Weighted opportunity value (value × probability) must round to a deterministic cent amount regardless of intermediate floating-point representation.                                                                       | value.cents=10001, probability=33.33 → weightedAmount = 100.01 * 0.3333 = 33.333333... → Math.round(33.333333*100)=3333 cents, but integer path gives Math.round(10001 \* 0.3333) = 3334 cents — one-cent discrepancy; On Money.create failure the getter silently returns the unweighted value                                         | fc.integer({min:1,max:10_000_000}) for value.cents; fc.float({min:0,max:100,noNaN:true}) for probability.value; fc.constantFrom('GBP','EUR','CAD','AUD','JPY')                                                                                                                                  | Opportunity.create({value: cents, probability: p}) → weightedValue.cents — assert equals Math.round(cents \* p/100); Verify weightedValue.cents <= value.cents for all p in [0,100]                                                                                                                       | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-05` | Low    | packages/domain/src/crm/billing/TaxRate.ts            | TaxRate.calculate(subtotal) must always return the correct tax amount; a computation failure must never silently produce zero tax.                                                                                         | TaxRate(33.33).calculate(Money(10000)) silently returns 0 if multiply fails, invoice is sent with no tax                                                                                                                                                                                                                                | fc.float({min:0,max:100,noNaN:true}) for rate; fc.integer({min:0,max:10_000_000}) for subtotal.cents; fc.constantFrom('GBP','EUR','CAD','AUD','JPY')                                                                                                                                            | TaxRate.create(r,'VAT') → calculate(Money.fromCents(n,'GBP')) — assert result.cents === Math.round(n \* r/100); TaxRate.zero().calculate(any) — assert result.cents === 0; forAll r in [0,100]: calculate(zero) === zero                                                                                  | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-07` | Low    | packages/domain/src/crm/billing/Invoice.ts            | Invoice numbers, receipt numbers, and ticket numbers must be unique within a process test run; numbers generated in parallel workers must not collide.                                                                     | Two Invoice.create() calls in the same module session produce the same invoiceNumber if the counter is at MAX_SAFE_INTEGER and wraps; invoiceNumber from test file A equals invoiceNumber from test file B when both run in same worker thread                                                                                          | fc.integer({min:1,max:100}) for batch size of create() calls; assert all resulting numbers are unique                                                                                                                                                                                           | N×Invoice.create() in sequence — assert all invoiceNumbers distinct; N×Receipt.create() in sequence — assert all receiptNumbers distinct; N×Ticket.create() in sequence — assert all ticketNumbers distinct                                                                                               | In production, replace module counter with DB sequence or UUID-based number | unit-mocked    | pure-property | smoke    | pure-domain | 1     |
| `RACE-PURE-08` | Low    | packages/domain/src/crm/lead/Lead.ts                  | A lead's status must only advance along defined transitions; e.g., CONVERTED→NEW must be rejected.                                                                                                                         | Lead{status:LOST}.changeStatus('NEW') succeeds — a lost lead appears fresh; Lead{status:UNQUALIFIED}.changeStatus('CONVERTED') succeeds — bypasses the qualify→convert path                                                                                                                                                             | fc.constantFrom('NEW','CONTACTED','QUALIFIED','NEGOTIATING','UNQUALIFIED','CONVERTED','LOST') for (from, to) pairs; property: if not explicitly allowed, changeStatus must fail                                                                                                                 | Lead.create → changeStatus(every possible target) — assert only valid pairs succeed; Lead qualified via qualify() vs changeStatus('QUALIFIED') — assert identical observable state                                                                                                                        | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-09` | Low    | packages/domain/src/crm/task/Task.ts                  | A task must not be completable from PENDING without passing through IN_PROGRESS; and no task should reach ARCHIVED except from COMPLETED or CANCELLED.                                                                     | Task{status:PENDING}.complete('user') succeeds — task bypasses IN_PROGRESS entirely; Task{status:IN_PROGRESS}.changeStatus('ARCHIVED','u') succeeds — bypasses archive() guard; Task{status:PENDING}.changeStatus('ARCHIVED','u') succeeds                                                                                              | fc.constantFrom('PENDING','IN_PROGRESS','COMPLETED','CANCELLED','ARCHIVED') for (from, to) pairs; define allowed-transitions map and property-test all pairs                                                                                                                                    | Task.create → complete — assert fails with TaskNotInProgressError; Task.create → start → complete — assert succeeds; Task.create → changeStatus('ARCHIVED') — assert fails with TaskCannotBeArchivedError                                                                                                 | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-10` | Low    | packages/domain/src/crm/ticket/Ticket.ts              | Priority changes must be rejected on any terminal ticket (CLOSED or ARCHIVED).                                                                                                                                             | Ticket{status:ARCHIVED}.changePriority('CRITICAL','user') succeeds; Ticket{status:ARCHIVED}.assign('agent','user') throws via raw throw (not Result) — inconsistent error style vs other methods                                                                                                                                        | fc.constantFrom('OPEN','IN_PROGRESS','WAITING_ON_CUSTOMER','WAITING_ON_THIRD_PARTY','RESOLVED','CLOSED','ARCHIVED') for from-status; fc.constantFrom('LOW','MEDIUM','HIGH','CRITICAL') for priority                                                                                             | Ticket.create → changeStatus sequence to ARCHIVED → changePriority — assert failure; Ticket.create → changeStatus to CLOSED → changePriority — assert failure (current guard)                                                                                                                             | none — pure domain invariant                                                | unit-mocked    | pure-property | smoke    | pure-domain | 1     |
| `RACE-PURE-11` | Low    | packages/domain/src/crm/ticket/Ticket.ts              | Cumulative slaPausedDuration must equal the sum of all individual pause intervals; resumeSla must not produce negative duration.                                                                                           | slaPausedDuration < 0 after resume with now < pausedAt; Multiple pause/resume cycles do not accumulate correctly if now values are non-monotonic                                                                                                                                                                                        | fc.array(fc.record({op:fc.constantFrom('pause','resume'),offsetMs:fc.integer({min:-60000,max:3600000})}),{minLength:2,maxLength:20}); property: final slaPausedDuration >= 0                                                                                                                    | create → pause(t0) → resume(t0+n) → assert slaPausedDuration === n; create → pause(t0) → resume(t0+a) → pause(t1) → resume(t1+b) → assert duration === a+b; create → pause(t0) → resume(t0-1) — assert rejection or clamping to 0                                                                         | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-12` | Low    | packages/domain/src/shared/ValueObject.ts             | ValueObject equality must be reflexive (v.equals(v)===true) and symmetric (v.equals(w)===w.equals(v)) for all valid instances.                                                                                             | Two TimeSlot instances holding dates differing only in milliseconds (e.g. from setSystemTime) may or may not be equal depending on how the dates were constructed                                                                                                                                                                       | fc.date() for startTime/endTime pairs; property: TimeSlot.create(s,e).equals(TimeSlot.create(s,e)) === true; TimeSlot.create(s1,e1).equals(TimeSlot.create(s2,e2)) === false iff s1!==s2 \|\| e1!==e2                                                                                           | v = ValueObject.create(x); assert v.equals(v) === true; v1 = create(x); v2 = create(x); assert v1.equals(v2) && v2.equals(v1); v1 = create(x); v2 = create(y) where x!==y; assert !v1.equals(v2)                                                                                                          | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-13` | Low    | packages/domain/src/crm/billing/PaymentTerms.ts       | Due date computed from an invoice issue date and payment terms must correspond to exactly daysUntilDue calendar days after the issue date.                                                                                 | In a DST-transition timezone, dueDate - issueDate may be 23 or 25 hours rather than exactly 24 hours, making daysUntilDue off by one when checked in wall-clock days                                                                                                                                                                    | fc.date({min:new Date('2020-01-01'),max:new Date('2030-12-31')}) for issueDate; fc.integer({min:0,max:365}) for daysUntilDue; property: dueDate.getTime() - issueDate.getTime() === daysUntilDue \* 86400000 (UTC-based check)                                                                  | PaymentTerms.create(d,'Net D') → calculateDueDate(issue) → assert (result-issue)/86400000 === d; forAll issue, days: assert result is (days) UTC-days after issue                                                                                                                                         | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-14` | Low    | packages/domain/src/legal/appointments/Appointment.ts | Appointments scheduled within 5 minutes of the current time must be accepted; appointments scheduled more than 5 minutes in the past must be rejected. This invariant must be testable without real wall-clock dependency. | Property test generates startTime = now+1ms → test passes; generates startTime = now-6min → test fails — non-deterministic across machines                                                                                                                                                                                              | fc.date({min: new Date(fixedNow - 5*60*1000 + 1), max: new Date(fixedNow + 365*86400*1000)}) for valid starts; fc.date({max: new Date(fixedNow - 5*60*1000 - 1)}) for invalid starts                                                                                                            | With vi.setSystemTime(T): Appointment.create({startTime: T+1ms}) → assert success; With vi.setSystemTime(T): Appointment.create({startTime: T-6min}) → assert AppointmentInPastError; Property: create with startTime in (now-5min, now+1year) always succeeds for valid other props                      | none — pure domain invariant                                                | unit-mocked    | pure-property | standard | pure-domain | 1     |
| `RACE-PURE-M1` | Low    | packages/domain/src/crm/ticket/Ticket.ts              | All Ticket mutation methods must return Result<void, DomainError> to allow uniform error handling; assignment must be rejected on all terminal statuses (CLOSED and ARCHIVED).                                             | Ticket{status:CLOSED}.assign('agent','user') — succeeds, closed ticket gets new assignee; Ticket{status:ARCHIVED}.unassign('user') — succeeds with no error, ARCHIVED ticket loses assignee silently                                                                                                                                    | fc.constantFrom('OPEN','IN_PROGRESS','WAITING_ON_CUSTOMER','WAITING_ON_THIRD_PARTY','RESOLVED','CLOSED','ARCHIVED') for status; property: assign/unassign must fail on CLOSED and ARCHIVED                                                                                                      | Ticket.create → changeStatus to CLOSED → assign('agent','user') — assert failure; Ticket.create → changeStatus sequence to ARCHIVED → unassign('user') — assert failure; Ticket.create → assign('a','u') → changeStatus to CLOSED → assign('b','u') — assert second assign fails                          | none — pure domain invariant                                                | unit-mocked    | pure-property | smoke    | pure-domain | 1     |
| `RACE-PURE-M2` | Low    | packages/domain/src/crm/task/Task.ts                  | A completed, cancelled, or archived task must not be re-linked to a different CRM entity; entity linkage is part of the task's final state.                                                                                | Task{status:COMPLETED}.assignToLead(newLeadId,'user') succeeds — completed task changes its CRM entity linkage; Task{status:ARCHIVED}.assignToOpportunity(oppId,'user') succeeds — archived task gets new linkage                                                                                                                       | fc.constantFrom('PENDING','IN_PROGRESS','COMPLETED','CANCELLED','ARCHIVED') for status; fc.string({minLength:1}) for entity IDs; property: linkage mutation must fail on terminal statuses                                                                                                      | Task.create → complete → assignToLead(leadId,'user') — assert failure; Task.create → cancel → assignToContact(contactId,'user') — assert failure; Task.create → start → complete → assignToOpportunity(oppId,'user') — assert failure                                                                     | none — pure domain invariant                                                | unit-mocked    | pure-property | smoke    | pure-domain | 1     |
| `RACE-PURE-M3` | Low    | packages/domain/src/crm/billing/Invoice.ts            | paymentStatus must be PARTIALLY_PAID whenever amountPaid > 0 and amountDue > 0, regardless of whether the invoice was previously PAID and reverted to OPEN via processRefund.                                              | After: issue → recordPayment(full) → processRefund(partial) → recordPayment(partial): paymentStatus='PARTIALLY_PAID' but amountRefunded > 0, losing refund signal; paymentStatus='PARTIALLY_REFUNDED' while status='PAID' — reachable if recordPayment completes the re-opened invoice without clearing refundedAmount signal correctly | fc.integer({min:1,max:10000}) for totalCents; fc.integer({min:1,max:totalCents}) for payAmount; fc.integer({min:1,max:payAmount}) for refundAmount; fc.integer({min:1,max:totalCents-payAmount+refundAmount}) for secondPayAmount                                                               | issue → recordPayment(full) → processRefund(partial) → recordPayment(refunded amount) → assert paymentStatus tracks combined paid+refunded state; issue → recordPayment(full) → processRefund(full) → assert status=OPEN, paymentStatus=REFUNDED, amountDue=totalAmount                                   | none — pure domain invariant                                                | unit-mocked    | model-based   | standard | pure-domain | 1     |

### quota-budget-capacity — Quota / Budget / Capacity — non-atomic decrement below zero

| ID              | Sev    | Aggregate / service                            | Business rule (invariant)                                                                                                                                                                                                                                                    | Invalid states                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Generated inputs (arbitraries)                                                                                                                                                                                     | Command sequence candidates                                                                                                                                                                                                                                                                                                                                                                   | Required DB constraints                                                                                                                            | Coverage today | Test type      | Tier     | Owner                 | Batch |
| --------------- | ------ | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | -------- | --------------------- | ----- |
| `RACE-QUOTA-01` | High   | packages/adapters/src/memory/zep/zep-client.ts | episodesUsed must never exceed maxEpisodes (1,000 for Zep Cloud free tier); every increment must be reflected atomically in the DB.                                                                                                                                          | Two ai-worker processes both see episodesUsed=999 at init, both call createSession, both write episodesUsed=1000 — one session is unrecorded in the DB count.; Concurrent addMemory calls across two adapter instances both read episodesUsed=950, both increment to 951, last writer wins: DB shows 951, real count is 952.; Adapter A writes 1001 while adapter B has already written 1001 independently — free-tier limit silently exceeded with no hard-limit enforcement. | fc.integer({ min: 990, max: 999 }) for initialEpisodes; fc.nat({ max: 5 }) for concurrentSessions; fc.scheduler() to interleave initialize → shouldUseFallback → createSession steps across N adapter instances.   | adapter1.initialize() \|\| adapter2.initialize() — both read 999; adapter1.createSession(s1) races adapter2.createSession(s2); assert DB.episodesUsed === 1001 (over limit)                                                                                                                                                                                                                   | ALTER TABLE zep_episode_usage ADD CONSTRAINT zep_episode_usage_count_range CHECK (episodesUsed >= 0 AND episodesUsed <= maxEpisodes);              | unit-mocked    | scheduler-race | standard | quota-budget-capacity | 2     |
| `RACE-QUOTA-03` | High   | apps/api/src/services/LeadRoutingService.ts    | An agent's currentCapacity must never exceed maxCapacity; the capacity guard and the increment must be atomic.                                                                                                                                                               | Agent has currentCapacity=9, maxCapacity=10; two routing transactions start concurrently; both read 9 < 10 and select the agent; both increment; DB shows currentCapacity=11.; Agent at capacity=10 (full); a concurrent routing and a manual re-route both pass the capacity filter at different instants and both increment; currentCapacity=12.                                                                                                                             | fc.integer({ min: 1, max: 10 }) for maxCapacity; fc.integer({ min: 0, max: 9 }) for initialCapacity; fc.nat({ max: 4 }) for concurrentRouteCount; real Postgres testcontainer to observe actual increment results. | getEligibleAgents(tenantId) — both txA and txB see agent with capacity=9; txA: updateMany increment; txB: updateMany increment; txA commit, txB commit; assert agentAvailability.currentCapacity <= agentAvailability.maxCapacity                                                                                                                                                             | ALTER TABLE agent_availability ADD CONSTRAINT agent_availability_capacity_ceiling CHECK (currentCapacity >= 0 AND currentCapacity <= maxCapacity); | unit-mocked    | db-concurrency | stress   | quota-budget-capacity | 2     |
| `RACE-QUOTA-M1` | High   | apps/api/src/services/LeadRoutingService.ts    | When a lead or ticket is resolved, closed, or reassigned, the previously assigned agent's currentCapacity must be decremented so their capacity slot is freed for future routing.                                                                                            | Agent routes 10 leads (maxCapacity=10); leads are resolved; currentCapacity stays at 10; agent is permanently excluded from all future routing even though they have no open assignments.; After N routing cycles, all agents reach maxCapacity and getEligibleAgents returns empty list, causing every new routeLead to throw 'No eligible agents available'.                                                                                                                 | fc.integer({ min: 1, max: 5 }) for maxCapacity; fc.array of route+resolve cycles                                                                                                                                   | routeLead(lead1) — agent.currentCapacity becomes 1; lead1.status = CONVERTED; routeLead(lead2) — agent.currentCapacity should be 1 (slot freed) but is still 1 (no decrement) if this is the only agent with maxCapacity=1                                                                                                                                                                    | none                                                                                                                                               | none           | db-concurrency | standard | quota-budget-capacity | 2     |
| `RACE-QUOTA-02` | Medium | apps/api/src/modules/zep/zep-budget.router.ts  | A reset operation must read the current episodesUsed, write the new count, and create the audit record atomically; the audit previousCount must always match what was in the DB at the moment of the update.                                                                 | Admin A reads previousCount=500, Admin B reads previousCount=500; A writes 0, B writes 0; audit shows two entries both claiming previousCount=500 — second reset lost the first reset in its previousCount.; Admin A reads 500 then is delayed; B resets to 0; A's audit records previousCount=500→250 but DB had already been 0 before A wrote 250.                                                                                                                           | fc.integer({ min: 0, max: 1000 }) for newCount; fc.scheduler() to interleave two concurrent reset mutations.                                                                                                       | resetA.findUnique() — reads 500; resetB.findUnique() — reads 500; resetA.upsert(0); resetB.upsert(250); assert audit previousCount on resetA entry is 500 and on resetB is 0 (not 500)                                                                                                                                                                                                        | None required for correctness; the $transaction fix is sufficient.                                                                                 | unit-mocked    | scheduler-race | standard | quota-budget-capacity | 2     |
| `RACE-QUOTA-M2` | Medium | packages/adapters/src/memory/zep/zep-client.ts | initialize() must execute exactly once per adapter instance; concurrent callers must not both load-then-overwrite episodeCount from the DB, which could set this.episodeCount to a stale value read by the slower caller after the faster caller has already incremented it. | Two concurrent createSession calls both invoke ensureInitialized(); both see isInitializedFlag=false; both call initialize(); both load episodesUsed=5 from DB; caller A finishes first, sets episodeCount=5 and isInitializedFlag=true, then increments to 6; caller B completes initialize() and sets this.episodeCount=5 again, losing caller A's increment in memory (DB still shows 6 until the next write overwrites it with 5).                                         | fc.scheduler() to interleave two concurrent createSession calls before initialize() sets isInitializedFlag                                                                                                         | adapter.createSession(s1) and adapter.createSession(s2) race; both call ensureInitialized() → both call initialize(); both load episodesUsed=5 from DB; s1 init completes: episodeCount=5, flag=true; s1 createSession: episodeCount++ → 6, writes DB=6; s2 init completes: episodeCount = record.episodesUsed = 5 (stale); s2 createSession: episodeCount++ → 6, writes DB=6 (double-counts) | none                                                                                                                                               | unit-mocked    | scheduler-race | standard | quota-budget-capacity | 2     |
| `RACE-QUOTA-M3` | Medium | apps/api/src/modules/zep/zep-budget.router.ts  | A reset must preserve the tenant's existing maxEpisodes, warningPercent, and hardLimitPercent; it must not overwrite them with hardcoded defaults when no record exists at the moment of the upsert.                                                                         | Tenant has custom maxEpisodes=500 stored in DB. Admin resets count. The upsert's update branch only sets episodesUsed, leaving maxEpisodes intact. But if the row was concurrently deleted between findUnique and upsert (or on first-ever reset), the create branch silently writes maxEpisodes=1000.                                                                                                                                                                         | fc.integer({ min: 100, max: 5000 }) for custom maxEpisodes; property: after any reset the maxEpisodes in DB must equal the pre-reset value                                                                         | tenant configures maxEpisodes=500; admin calls reset mutation; upsert hits update branch: episodesUsed=0, maxEpisodes unchanged — correct; row deleted externally (e.g. test teardown); admin calls reset again: upsert hits create branch: maxEpisodes=1000 (wrong)                                                                                                                          | none                                                                                                                                               | unit-mocked    | scheduler-race | standard | quota-budget-capacity | 2     |

### test-infra — Test infrastructure inventory — what exists vs what Phase 1 must add

| ID              | Sev    | Aggregate / service               | Business rule (invariant)                                                                                                                                                                                     | Invalid states                                                                                                                                                                                                                                                                                                                 | Generated inputs (arbitraries)                                                                                                                                                                                  | Command sequence candidates | Required DB constraints | Coverage today | Test type      | Tier     | Owner      | Batch |
| --------------- | ------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------- | -------------- | -------------- | -------- | ---------- | ----- |
| `RACE-TEST--M1` | High   | tests/integration/setup.ts        | All PrismaClient constructors in a Prisma 7 + engineType='client' project MUST pass { adapter: new PrismaPg({ connectionString }) }; constructing without an adapter throws at runtime.                       | new PrismaClient({ datasources: { db: { url } } }) throws 'PrismaClientInitializationError: The datasources option is not supported with the new engineType client' at runtime; All integration tests that reach the PrismaClient constructor silently skip (infra unavailable guard) rather than surface the misconfiguration | N/A — infrastructure construction bug, not a property test target                                                                                                                                               |                             | none                    | none           | db-concurrency | smoke    | test-infra | 1     |
| `RACE-TEST--M2` | Medium | tests/integration/setup.ts        | Each integration test must run against isolated DB state; with maxWorkers=4, multiple test files execute concurrently against the same PostgreSQL container with no per-file transaction or schema isolation. | Worker A's beforeEach TRUNCATE races with Worker B's INSERT — FK violation aborts B's test; Two workers both INSERT a unique-keyed record (e.g. same tenantId) — one silently wins, the other gets a P2002 Prisma error unrelated to its actual test logic                                                                     | N/A — infrastructure concurrency gap, not a domain property test                                                                                                                                                |                             | none                    | none           | db-concurrency | standard | test-infra | 1     |
| `RACE-TEST--M3` | Medium | scripts/run-tests.js              | The pnpm test script must surface ALL test failures to CI; if any project reports failures that are overwritten by a later 'X passed' stdout line, CI can exit 0 while tests are red.                         | run-tests.js reports '0 failed' in its summary while vitest exited 1 — developers see contradictory signals; The parsed testsPassedCount reflects only the last vitest summary line, not the total across all 17 projects                                                                                                      | N/A — CI reporting gap, not a domain property test                                                                                                                                                              |                             | none                    | none           | pure-property  | smoke    | test-infra | 1     |
| `RACE-TEST--01` | Low    | package.json                      | Property-based tests require fast-check to generate arbitrary inputs; without it no invariant can be driven by random data in CI.                                                                             | No fc.assert/test.prop calls can compile or run; CI will reject any property test file at import time                                                                                                                                                                                                                          | N/A — this is a dependency gap, not a domain finding                                                                                                                                                            |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |
| `RACE-TEST--02` | Low    | tests/property/                   | A shared property-test support tree (arbitraries, commands, model, scheduler, database, assertions) must exist before any other lane can write property tests.                                                | No shared fc.Arbitrary<Lead>, fc.Arbitrary<Opportunity>, etc.; No model-based command wrappers for domain aggregates; No scheduler-race harness for concurrent tRPC calls; No db-concurrency sandbox helper                                                                                                                    | fc.record({ email: fc.emailAddress(), score: fc.integer({min:0,max:100}), status: fc.constantFrom('NEW','CONTACTED','QUALIFIED','NEGOTIATING','UNQUALIFIED','CONVERTED','LOST') }) for Lead arbitraries         |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |
| `RACE-TEST--03` | Low    | tests/integration/setup.ts        | True concurrent-write tests (e.g. duplicate-event delivery, parallel upsert races) must use separate Prisma connections so row-level locks are not serialized on a shared client.                             | Two 'concurrent' test clients are actually serialized through one pg connection; FOR UPDATE SKIP LOCKED tests appear to pass even when lock is not respected                                                                                                                                                                   | N/A — infrastructure helper, not a domain arbitrary                                                                                                                                                             |                             | none                    | none           | db-concurrency | standard | test-infra | 0     |
| `RACE-TEST--04` | Low    | tests/utils/db.ts                 | The shared database test helper must provide real transaction/rollback isolation; the current implementation is a no-op skeleton that gives a false sense of coverage.                                        | withTestTransaction() provides {} instead of a real Prisma transaction client; Tests using this helper pass but make zero DB assertions                                                                                                                                                                                        | N/A                                                                                                                                                                                                             |                             | none                    | none           | db-concurrency | standard | test-infra | 0     |
| `RACE-TEST--05` | Low    | package.json                      | Property and concurrency test tiers must be addressable by CI workflows via stable npm script names; without them CI cannot selectively gate PRs on smoke vs stress budgets.                                  | CI cannot run property tests because there is no script to call; Tier separation (smoke/standard/stress) cannot be enforced in PR vs nightly workflows                                                                                                                                                                         | N/A                                                                                                                                                                                                             |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |
| `RACE-TEST--06` | Low    | .github/workflows/ci.yml          | Property tests must be wired to CI: smoke tier on every PR (fast, seed-deterministic), standard on every main push, stress in the nightly schedule — currently none of these jobs exist.                      | Property invariant regressions can merge undetected because no CI gate exists; Stress-tier findings (e.g. unbounded retry loops, large-input edge cases) will never be discovered in CI                                                                                                                                        | N/A                                                                                                                                                                                                             |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |
| `RACE-TEST--07` | Low    | packages/test-fixtures/src/ids.ts | Shared property tests need reusable fast-check arbitraries and domain object builders; the fixture package currently exports only 7 static UUID constants.                                                    | No fc.Arbitrary<CreateLeadProps> to drive Lead property tests; No builder factories for Opportunity, Contact, Ticket, etc.; Each test package must independently reinvent domain builders leading to drift                                                                                                                     | fc.record({ email: fc.emailAddress(), source: fc.constantFrom('WEBSITE','REFERRAL','SOCIAL','EMAIL','COLD_CALL','EVENT','OTHER'), ownerId: fc.constant(TEST_OWNER_ID), tenantId: fc.constant(TEST_TENANT_ID) }) |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |
| `RACE-TEST--08` | Low    | vitest.config.ts                  | When a property test fails in CI, engineers must be able to replay the exact failing seed locally; without a seed reporter and replay instructions, CI failures are non-reproducible.                         | A failing property test in CI produces a shrunk counterexample but the seed is lost after the run; Re-running the job does not guarantee the same seed unless FC_SEED is pinned                                                                                                                                                | N/A                                                                                                                                                                                                             |                             | none                    | none           | pure-property  | smoke    | test-infra | 0     |

### webhooks-outbox-idempotency — Webhooks / Outbox — duplicate event effects, exactly-once dispatch

| ID              | Sev    | Aggregate / service                                          | Business rule (invariant)                                                                                                                                                                          | Invalid states                                                                                                                                                                                                                                                                                                                                                                    | Generated inputs (arbitraries)                                                                                                                                                                                                                                           | Command sequence candidates                                                                                                                                                                                                                                  | Required DB constraints                                                                                                                                                                                                                                                                                            | Coverage today | Test type      | Tier     | Owner                       | Batch |
| --------------- | ------ | ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------- | -------- | --------------------------- | ----- |
| `RACE-WEBHO-01` | High   | packages/adapters/src/events/OutboxEventBusAdapter.ts        | A domain event with the same idempotency key must appear at most once in the outbox.                                                                                                               | Two concurrent publish() calls with the same eventId both return null from findFirst and both succeed in create, inserting two PENDING rows with the same idempotency key.; Downstream poller processes both rows, emitting duplicate side-effects (e.g., two lead-scored notifications, two billing charges).                                                                    | fc.record({ eventType: fc.constantFrom('lead.created','opportunity.won'), aggregateId: fc.uuid(), eventId: fc.uuid() }) — run N concurrent publish() calls with the same eventId via Promise.all on two separate PrismaClient connections.                               | T1: findFirst(idempotencyKey) → T2: findFirst(idempotencyKey) → T1: create → T2: create (both succeed); T1: findFirst(null) → T1: create → T2: findFirst(null, read committed) → T2: create                                                                  | @@unique([tenantId, metadata]) is not directly expressible for JSONB paths in Prisma; the fix is a generated column + unique index on domain_events(tenantId, (metadata->>'idempotencyKey')) or wrapping the findFirst+create in a $transaction with serializable isolation.                                       | unit-mocked    | db-concurrency | standard | webhooks-outbox-idempotency | 1     |
| `RACE-WEBHO-02` | High   | apps/workers/events-worker/src/outbox/pollOutbox.ts          | An outbox event dispatched to handlers must be marked PROCESSED in the same atomic unit; a crash between dispatch and acknowledgement must not cause double-dispatch.                              | Poller claims row, dispatches event (side-effect delivered), process crashes before markAsPublished; next poll cycle re-dispatches same event.; markAsPublished itself throws a transient DB error after dispatch; scheduleRetry puts the row back to PENDING with incremented retryCount, causing re-dispatch.                                                                   | fc.record({ eventId: fc.uuid(), dispatchSucceeds: fc.boolean(), markPublishedThrows: fc.boolean() }) — model-based test that exercises all four combinations of (dispatch, markPublished) success/failure.                                                               | fetchPending → dispatch (succeeds) → markAsPublished (throws) → scheduleRetry → fetchPending → dispatch (duplicate); fetchPending → dispatch (succeeds) → process crash → restart → fetchPending → dispatch (duplicate)                                      | Add a status transition to PROCESSING immediately before dispatch inside a transaction (compare-and-swap: UPDATE … WHERE status='PENDING' AND id=? SET status='PROCESSING'). If dispatch succeeds, transition to PROCESSED. If PROCESSING rows are found on restart, use a lock-timeout to dead-letter stale ones. | unit-mocked    | model-based    | standard | webhooks-outbox-idempotency | 1     |
| `RACE-WEBHO-M1` | High   | packages/adapters/src/events/OutboxEventBusAdapter.ts        | publishAll must guarantee at-most-one outbox row per idempotency key even under concurrent batch publishes.                                                                                        | Two concurrent publishAll() calls with overlapping eventIds both run findFirst inside their own READ COMMITTED transactions, both observe no existing row, both create a row — duplicate PENDING rows despite the transaction wrapper.                                                                                                                                            | fc.array(fc.record({ eventId: fc.uuid(), eventType: fc.constantFrom('lead.created','opportunity.won') }), { minLength: 2, maxLength: 5 }) — run two concurrent publishAll() calls sharing at least one event via Promise.all with two separate PrismaClient connections. | T1: BEGIN (READ COMMITTED) → T1: findFirst(key)=null → T2: BEGIN → T2: findFirst(key)=null → T1: create → T1: COMMIT → T2: create → T2: COMMIT (both rows exist)                                                                                             | Unique index on (tenantId, (metadata->>'idempotencyKey')) OR use $transaction({ isolationLevel: Prisma.TransactionIsolationLevel.Serializable }).                                                                                                                                                                  | unit-mocked    | db-concurrency | standard |                             |       |
| `RACE-WEBHO-M3` | High   | packages/adapters/src/repositories/PrismaOutboxRepository.ts | A row claimed by FOR UPDATE SKIP LOCKED must remain locked until the processing result (PROCESSED or FAILED) is committed, preventing a second poller from claiming it during the dispatch window. | Poller A fetches row (lock held during $queryRaw, released immediately after). Poller B runs fetchPendingEvents within 100ms — row is still PENDING, lock is no longer held by A. Poller B also gets the row. Both pollers dispatch the event.                                                                                                                                    | Two separate PrismaClient connections running fetchPendingEvents simultaneously against a real postgres:15-alpine container; assert each row appears in exactly one result set.                                                                                          | A: $queryRaw FOR UPDATE SKIP LOCKED → A: lock released (autocommit) → B: $queryRaw FOR UPDATE SKIP LOCKED → B: gets same row → A: dispatch → B: dispatch (duplicate)                                                                                         | Wrap fetchPendingEvents inside an explicit $transaction that stays open until markAsPublished is committed, OR write status=PROCESSING atomically before releasing the lock so a second poller skips the row on its WHERE status='PENDING' filter.                                                                 | unit-mocked    | db-concurrency | standard |                             |       |
| `RACE-WEBHO-03` | Medium | packages/webhooks/src/framework.ts                           | A webhook event delivered twice (same event.id from same source) must execute handlers exactly once per framework instance.                                                                        | Two concurrent handle() calls with the same (sourceName, event.id) both pass the has() check before either writes via set(); both execute all handlers; business side-effects are doubled.                                                                                                                                                                                        | fc.record({ sourceName: fc.string(), eventId: fc.string(), concurrency: fc.integer({ min: 2, max: 8 }) }) — use Promise.all to fire N simultaneous handle() calls with the same eventId; count handler invocations and assert === 1.                                     | T1: has(key)=false → T2: has(key)=false → T1: executeHandlers → T2: executeHandlers → T1: set(key) → T2: set(key)                                                                                                                                            | None — in-memory store; fix is to set the key to a Promise sentinel immediately on first check (pre-mark pattern) and short-circuit concurrent callers that see the pending sentinel.                                                                                                                              | unit-mocked    | scheduler-race | standard | webhooks-outbox-idempotency | 1     |
| `RACE-WEBHO-04` | Medium | packages/adapters/src/events/OutboxEventBusAdapter.ts        | publishAll must write all events atomically using the same database connection/transaction as the calling domain service to guarantee the outbox pattern.                                          | Consumer passes a tenant-specific PrismaClient as `prisma` option; publishAll silently writes to the singleton's DB connection instead; events are written to the wrong tenant's connection or a different isolation context.; Integration test creates a PrismaClient pointing to a test DB; publishAll writes to the application DB because withTransaction uses the singleton. | fc.record({ tenantId: fc.uuid() }) — property: for any injected prisma client, the transaction in publishAll must run on that same client, not an external singleton.                                                                                                    | Inject test PrismaClient A → call publishAll → observe transaction runs on singleton client B                                                                                                                                                                | None — pure code-architecture fix.                                                                                                                                                                                                                                                                                 | unit-mocked    | pure-property  | smoke    | webhooks-outbox-idempotency | 1     |
| `RACE-WEBHO-05` | Medium | packages/webhooks/src/framework.ts                           | Manually reprocessing a dead-letter event must not produce duplicate handler effects if called concurrently or if the event was already successfully handled.                                      | Two concurrent reprocessDeadLetter('eventId') calls both find the entry, both call deadLetterQueue.remove(), both execute handlers — doubling side-effects.                                                                                                                                                                                                                       | fc.record({ eventId: fc.string() }) — schedule two simultaneous reprocessDeadLetter calls with the same eventId; assert handler called exactly once.                                                                                                                     | T1: find(eventId) found → T2: find(eventId) found → T1: remove → T2: remove → T1: handlers → T2: handlers                                                                                                                                                    | None — in-memory fix.                                                                                                                                                                                                                                                                                              | unit-mocked    | scheduler-race | standard | webhooks-outbox-idempotency | 1     |
| `RACE-WEBHO-M2` | Medium | packages/webhooks/src/framework.ts                           | A retried webhook event whose idempotency key is already marked as processed must not be executed again by the normal handle() path or a concurrent retry.                                         | Event is in retryQueue. handle() is called concurrently with processRetries(). processRetries() begins executing handlers without checking has(). handle() passes has()=false (not yet set) and also executes handlers. Both complete and both set idempotency.set(). Handler side-effects duplicated.                                                                            | fc.record({ eventId: fc.string(), sourceName: fc.string() }) — schedule simultaneous processRetries() and handle() with the same eventId; count total handler invocations and assert === 1.                                                                              | T1: processRetries → getReady (entry removed from queue) → executeHandlers (no has() check) → T2: handle → has(key)=false (not yet set) → executeHandlers → T1: set(key) → T2: set(key)                                                                      | None — in-memory fix.                                                                                                                                                                                                                                                                                              | unit-mocked    | scheduler-race | standard |                             |       |
| `RACE-WEBHO-06` | Low    | packages/adapters/src/events/OutboxEventBusAdapter.ts        | An outbox event must be written in the same transaction as the domain aggregate state change that triggered it — a crash between aggregate save and event write must not produce a ghost event.    | findFirst returns null (no duplicate) → DB error between findFirst and create → event is not written but caller received no error (if error is swallowed upstream) → event lost.; Concurrent callers both pass findFirst (RACE-WEBHO-01) → both write duplicate rows.                                                                                                             | fc.record({ eventType: fc.string(), aggregateId: fc.uuid() }) — property: for any event, after publish() completes without throwing, exactly one row with the matching idempotencyKey exists in the outbox.                                                              | findFirst (no duplicate) → DB write fails → caller sees error → no row written (benign but event lost if not retried); findFirst (no duplicate) → create succeeds → concurrent findFirst (reads committed: still no dup) → concurrent create (duplicate row) | As per RACE-WEBHO-01: unique index on (tenantId, idempotencyKey) would make the create fail on duplicate and remove the need for a pre-check entirely.                                                                                                                                                             | unit-mocked    | pure-property  | smoke    | webhooks-outbox-idempotency | 1     |

### workers-queue-jobs — Queue / Background jobs — at-most-once business effect, retry idempotency

| ID              | Sev    | Aggregate / service                                          | Business rule (invariant)                                                                                                                                                                   | Invalid states                                                                                                                                                                                                                                                                              | Generated inputs (arbitraries)                                                                                                                                                                                                                                                                  | Command sequence candidates                                                                                                                                                                                                                                                                                            | Required DB constraints                                                                                                                                                                                                                    | Coverage today | Test type      | Tier     | Owner              | Batch |
| --------------- | ------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------------- | -------- | ------------------ | ----- |
| `RACE-WORKE-01` | High   | apps/workers/events-worker/src/maintenance/scheduled-jobs.ts | Each ticket SLA breach must produce exactly one notification per worker run; duplicate notifications must not be delivered to the assignee.                                                 | Ticket slaStatus = BREACHED but two BREACHED notifications exist for the same ticket in the same run window; slaBreachedAt set to two different timestamps on the same ticket; Assignee receives duplicate SLA breach push notifications                                                    | fc.record({ ticketId: fc.uuid(), slaResolutionDue: fc.date({ max: new Date() }), slaStatus: fc.constant('ON_TRACK'), assigneeId: fc.uuid(), tenantId: fc.uuid() }); drive two concurrent checkSLABreaches() calls against the same Prisma client                                                | T1: prisma.ticket.findMany → [ticket_A]; T2: prisma.ticket.findMany → [ticket_A]; T1: ticket.update(BREACHED); T2: ticket.update(BREACHED); T1: notification.create; T2: notification.create → duplicate notification; Single instance, interval fires at t=0 and t=60s, first run still processing at t=60s → overlap | Add @@unique([tenantId, sourceId, sourceType]) to Notification model, OR wrap the findMany+update+create block in $transaction with serializable isolation.                                                                                | none           | scheduler-race | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-04` | High   | packages/platform/src/queues/queue-factory.ts                | The global retry budget must actually decrement on each enqueue so that the burst-protection ceiling is enforced within the sliding window.                                                 | A burst of 10,000 scoring enqueue calls within one window succeeds even though defaultBudget=100 should have stopped at 100; Thundering-herd from a deployment restart saturates the Redis scoring queue without any backpressure                                                           | fc.integer({ min: 1, max: 500 }) for call count; assert that after N > defaultBudget (100) calls to enqueueAIScoring in the same window, at least (N - 100) of them were rejected — currently this property fails                                                                               | Call enqueueAIScoring 150 times within one windowMs; expect first 100 to succeed and next 50 to throw; currently all 150 succeed                                                                                                                                                                                       | none — pure in-process fix: add globalRetryBudget.consumeRetry(QUEUE_NAMES.AI_SCORING) immediately after the canRetry guard passes                                                                                                         | unit-mocked    | pure-property  | smoke    | workers-queue-jobs | 3     |
| `RACE-WORKE-M3` | High   | packages/adapters/src/repositories/PrismaOutboxRepository.ts | Each domain event in the outbox must be dispatched exactly once across all poller instances; double-dispatch causes duplicate side effects (duplicate notifications, double billing, etc.). | Same domain event dispatched twice to subscription-bridge handlers — e.g., LeadScored emitted twice, causing duplicate real-time notifications to connected clients; notificationId from enqueueNotification called twice for the same event — duplicate notification delivery to end users | fc.record({ eventId: fc.uuid(), eventType: fc.constantFrom('lead.scored', 'task.assigned') }); simulate dispatch succeeding then markAsPublished throwing; assert handler called exactly once across retry                                                                                      | Poller fetches event-E (FOR UPDATE lock held); dispatch(event-E) succeeds; markAsPublished(event-E) throws DB error; lock released; next poll re-fetches event-E; dispatch(event-E) called again — duplicate side effect                                                                                               | Move markAsPublished into the same transaction as a status-conditional update: UPDATE domain_events SET status='PROCESSED' WHERE id=$1 AND status='PROCESSING' — skip dispatch if update count = 0 (already processed by another instance) | unit-mocked    | db-concurrency | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-02` | Medium | apps/workers/events-worker/src/maintenance/scheduled-jobs.ts | A stale-lead, overdue-task, stale-deal, or appointment-reminder notification must be sent at most once per dedup window regardless of concurrent scheduler instances.                       | Recipient receives multiple identical stale-lead reminder emails/notifications within the dedup window; Appointment organizer receives N copies of the 15-minute reminder                                                                                                                   | fc.record({ leadId: fc.uuid(), ownerId: fc.uuid(), tenantId: fc.uuid(), updatedAt: fc.date({ max: subtractDays(new Date(), 8) }) }); run two concurrent checkFollowUpReminders() coroutines with shared Prisma client and assert notification count === 1                                       | T1: notification.findFirst({sourceId: lead.id, createdAt: >=yesterday}) → null; T2: same → null; T1: notification.create; T2: notification.create → duplicate                                                                                                                                                          | Partial unique index: @@unique([tenantId, sourceId, sourceType]) WHERE status IN ('PENDING','SENT','DELIVERED') — or use upsert with an ignored conflict key                                                                               | none           | scheduler-race | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-03` | Medium | apps/ai-worker/src/jobs/insight-generation.job.ts            | For a given (tenantId, title, entityId) combination, only one active AIInsight row should exist at a time.                                                                                  | Multiple NEW AIInsight rows with identical (tenantId, title, entityId) — dashboard shows duplicate cards; Notification created twice for the same critical insight in persistInsightNotification (insight-generation.job.ts:611-666)                                                        | fc.record({ tenantId: fc.uuid(), title: fc.string(), entityId: fc.uuid(), entityType: fc.constantFrom('lead', 'contact', 'opportunity') }); race two processInsightJob calls with shared DB                                                                                                     | Job-A (tenant-1): aIInsight.findFirst → null; Job-B (tenant-1, same insights): aIInsight.findFirst → null; Job-A: aIInsight.create; Job-B: aIInsight.create → duplicate                                                                                                                                                | Add @@unique([tenantId, title]) or @@unique([tenantId, entityId, entityType]) filtered by active status, then use upsert with updateMany({status:'DISMISSED'}) on conflict.                                                                | unit-mocked    | db-concurrency | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-06` | Medium | apps/ai-worker/src/account-scoring.chain.ts                  | The account score written by a higher-quality (LLM) scoring run must not be silently overwritten by a concurrently-completing lower-quality (fallback) run.                                 | A 'fallback' score (0) overwrites a valid LLM score (e.g. 72) on the account row; scoreModelVersion = 'account-scoring-v1' (fallback) after a successful LLM run was committed first                                                                                                        | fc.record({ accountId: fc.uuid(), tenantId: fc.uuid(), score1: fc.integer({min:50,max:100}), score2: fc.integer({min:0,max:40}), t1Completes: fc.boolean() }); model two concurrent scoreAccount calls with shared prisma mock; assert final score equals the one from the later-resolving call | Job-A starts LLM call for account-X (takes 5s); Job-B starts fallback path for same account-X (takes 0.1s); B commits score=0; A commits score=75 — final is 75 (A wins); if order reverses, final is 0 (B wins) — non-deterministic                                                                                   | Add a scored_at-based conditional: only UPDATE when scoredAt IS NULL OR scoredAt < incoming.scoredAt, or add an integer version column.                                                                                                    | none           | db-concurrency | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-M1` | Medium | apps/ai-worker/src/jobs/insight-generation.job.ts            | For a given (tenantId, recipientId, sourceId/subject) combination, only one AI insight notification must be delivered per insight generation cycle.                                         | User receives N copies of the same AI insight in-app notification within one job retry cycle; Notification inbox shows duplicate 'Hot lead identified' entries for the same insight                                                                                                         | fc.record({ tenantId: fc.uuid(), userId: fc.uuid(), entityId: fc.uuid(), title: fc.string() }); run two concurrent persistInsightNotification calls; assert notification count for (tenantId, recipientId, subject) === 1                                                                       | Job-A: notification.findFirst({subject: insight.title}) → null; Job-B: same → null; Job-A: notification.create; Job-B: notification.create → 2 rows                                                                                                                                                                    | @@unique([tenantId, recipientId, sourceId, sourceType]) partial index WHERE status NOT IN ('DISMISSED') on Notification, then switch to upsert with ON CONFLICT DO NOTHING                                                                 | unit-mocked    | db-concurrency | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-M2` | Medium | apps/ai-worker/src/jobs/account-scoring.job.ts               | Each account must have at most one active scoring job in flight at a time to prevent the lost-update race in account-scoring.chain.ts.                                                      | Two score jobs for account-X both in ACTIVE state; whichever commits last overwrites the other — final score is non-deterministic; Account score shows 0 (fallback) after a successful LLM run was committed first and then overwritten                                                     | fc.record({ accountId: fc.uuid(), tenantId: fc.uuid() }); enqueue two score jobs with the same accountId and shared Queue mock; assert queue.add was called with a deterministic jobId that BullMQ would deduplicate                                                                            | Tick-1 fan-out enqueues job-X for account-A; Tick-2 fan-out (before Tick-1 job completes) enqueues job-Y for account-A; both workers pick up in parallel; last commit wins                                                                                                                                             | none additional — fix is a deterministic jobId: `score-account-${accountId}-${tenantId}-${dayBucket}`                                                                                                                                      | none           | scheduler-race | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-05` | Low    | apps/ai-worker/src/jobs/scoring.job.ts                       | A cron-triggered scoring or insight dispatcher must not enqueue the same lead/tenant job more than once per cron tick, even if two cron instances overlap.                                  | N \* leads score-lead jobs in queue after a sentinel retry, where N is the number of fan-out leads — worker backlog grows proportionally to retries; Same lead scored twice in rapid succession, second write overwrites first with stale LLM output                                        | fc.array(fc.uuid(), { minLength: 1, maxLength: 20 }) for leadIds; simulate sentinel job running twice with shared Queue mock; assert queue.add was called with each leadId exactly once                                                                                                         | Sentinel job picked by worker-1, handleScheduledDispatch enqueues lead-A..lead-Z; sentinel job stalls (lock expires); sentinel retried by worker-2 → lead-A..lead-Z enqueued again                                                                                                                                     | none additional — upsert is already idempotent at DB level                                                                                                                                                                                 | none           | scheduler-race | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-07` | Low    | apps/ai-worker/src/jobs/insight-generation.job.ts            | Each AI insight must produce at most one activity feed entry per (leadId/contactId, title, tenantId) combination.                                                                           | Lead activity feed shows duplicate '[AI] Hot lead' entries; Contact timeline shows duplicate AI insight entries with identical content                                                                                                                                                      | fc.record({ leadId: fc.uuid(), tenantId: fc.uuid(), title: fc.string() }); run two concurrent persistEntityActivity calls; assert LeadActivity count for (leadId, title) === 1                                                                                                                  | Job-A: leadActivity.findFirst({leadId, title}) → null; Job-B: same → null; Job-A: leadActivity.create; Job-B: leadActivity.create → 2 rows                                                                                                                                                                             | @@unique([leadId, title, tenantId]) on LeadActivity (partial: WHERE type='SCORE_UPDATE'), similar for ContactActivity                                                                                                                      | unit-mocked    | db-concurrency | standard | workers-queue-jobs | 3     |
| `RACE-WORKE-08` | Low    | packages/platform/src/queues/retry-strategy.ts               | The in-process retry budget must not allow more enqueues than the configured defaultBudget within one windowMs, even when multiple async callers race through canRetry+consumeRetry.        | Up to (concurrency factor) \* defaultBudget enqueues succeed in one window instead of exactly defaultBudget                                                                                                                                                                                 | fc.integer({ min: 2, max: 20 }) for concurrent caller count; assert sum of successful enqueues <= defaultBudget after Promise.all(N x enqueueAIScoring)                                                                                                                                         | 10 concurrent callers each call canRetry() → all see remaining=100 → all await queue.add → all call consumeRetry() → remaining decremented 10x from 100 to 90, but all 10 succeeded (correct if sequential, but budget not atomically checked-and-decremented)                                                         | none — in-process only                                                                                                                                                                                                                     | unit-mocked    | pure-property  | smoke    | workers-queue-jobs | 3     |

## Pure-domain property candidates

Low/Medium severity — no concurrency risk, but valuable invariants for
`fast-check` `test.prop` (domain layer, no Prisma).

| Object                                              | File                                                                                                         | Properties                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Severity | Lane                        |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------- |
| DurableAuditLogAdapter hash chain                   | packages/adapters/src/audit/DurableAuditLogAdapter.ts                                                        | For any sequence of N events logged sequentially, event[i].previousHash === event[i-1].integrityHash for all i > 0; event[0].previousHash === 'GENESIS'; For any logged event, verifyLogIntegrity(eventId).valid === true when the row has not been tampered with; computeHash is deterministic: same input always yields same hex string                                                                                                                                                                                                                                                                                        | Critical | audit-log                   |
| ScheduleAppointmentUseCase — conflict gate          | packages/application/src/usecases/scheduling/ScheduleAppointment.ts                                          | execute() with forceOverrideConflicts=false and non-empty conflicts never calls appointmentRepository.save; execute() with forceOverrideConflicts=true always calls appointmentRepository.save regardless of conflicts; A slot that does not overlap any existing appointment is always saved (no false-positive conflict rejection)                                                                                                                                                                                                                                                                                             | High     | booking-scheduling          |
| IdempotencyStore                                    | packages/webhooks/src/framework.ts                                                                           | after N concurrent handle() calls with the same (source, eventId), handler is invoked exactly once; has() returning false then set() then has() always returns true within the TTL; cleanup() removes all entries older than ttlMs and returns correct removed count; has() after TTL expiry always returns false regardless of prior set()                                                                                                                                                                                                                                                                                      | High     | webhooks-outbox-idempotency |
| OutboxEventBusAdapter.publish                       | packages/adapters/src/events/OutboxEventBusAdapter.ts                                                        | for any event, after N concurrent publish() calls with identical eventId, exactly one row exists in domain_events; idempotencyKey format is always eventType:aggregateId:eventId with no empty segments; publish() with duplicate eventId never throws — it returns silently                                                                                                                                                                                                                                                                                                                                                     | High     | webhooks-outbox-idempotency |
| PrismaOutboxRepository.fetchPendingEvents           | packages/adapters/src/repositories/PrismaOutboxRepository.ts                                                 | for any batch size N, the union of results from M concurrent fetchPendingEvents calls contains no duplicates; an event with nextRetryAt in the future is never returned; limit is always respected — result.length <= limit                                                                                                                                                                                                                                                                                                                                                                                                      | High     | webhooks-outbox-idempotency |
| MaintenanceScheduler.checkSLABreaches               | apps/workers/events-worker/src/maintenance/scheduled-jobs.ts                                                 | For any ticket T, after a single run of checkSLABreaches, the count of BREACHED notifications for T in sourceType='sla_breach' is exactly 1 (not 0, not 2+); A ticket with slaStatus=BREACHED is never re-updated or re-notified on the next run                                                                                                                                                                                                                                                                                                                                                                                 | High     | workers-queue-jobs          |
| ZepMemoryAdapter.episodeCount                       | packages/adapters/src/memory/zep/zep-client.ts                                                               | After N concurrent createSession calls starting from initialCount, DB episodesUsed === initialCount + N (no over- or under-count); DB episodesUsed is always <= maxEpisodes after any sequence of createSession/addMemory calls; audit entries delta values sum to the total change from initialCount to final episodesUsed                                                                                                                                                                                                                                                                                                      | High     | quota-budget-capacity       |
| AgentAvailability.currentCapacity                   | apps/api/src/services/LeadRoutingService.ts                                                                  | After N concurrent routeLead/routeTicket calls, for every agent: currentCapacity <= maxCapacity; currentCapacity >= 0 at all times; Sum of increments across all routing transactions equals the number of successful assignments                                                                                                                                                                                                                                                                                                                                                                                                | High     | quota-budget-capacity       |
| PrismaTenantModuleRepository toggle sequence        | packages/adapters/src/repositories/PrismaTenantModuleRepository.ts                                           | After any finite sequence of enableModule / disableModule calls for the same (tenantId, moduleId), the final enabled state equals the last operation's intent; CORE_CRM.enabled is always true regardless of disableModule calls; isModuleEnabled result is consistent with the last committed upsert for that (tenantId, moduleId)                                                                                                                                                                                                                                                                                              | High     | entitlement-modules         |
| Money                                               | packages/domain/src/shared/Money.ts                                                                          | add is commutative: fromCents(a,c).add(fromCents(b,c)).cents === fromCents(b,c).add(fromCents(a,c)).cents; add is associative: (a+b)+c cents === a+(b+c) cents; fromCents round-trips: fromCents(n,c).cents === n for all n≥0; create decimal round-trips: create(x,c).cents === Math.round(x\*100) for non-zero-decimal currencies; currency must be in SUPPORTED_CURRENCIES after normalization; SUPPORTED_CURRENCIES has no duplicates and includes USD                                                                                                                                                                       | High     | pure-domain                 |
| Invoice (pay/refund accounting invariant)           | packages/domain/src/crm/billing/Invoice.ts                                                                   | amountPaid.cents + amountDue.cents - amountRefunded.cents === totalAmount.cents after any command sequence; status === PAID iff amountDue.cents === 0; paymentStatus === REFUNDED iff amountRefunded.cents === amountPaid.cents; amountDue.cents >= 0 at all times; addLineItem then removeLineItem(same index) returns totalAmount to original                                                                                                                                                                                                                                                                                  | High     | pure-domain                 |
| Appointment domain aggregate — status state machine | packages/domain/src/legal/appointments/Appointment.ts                                                        | confirm() on a CANCELLED appointment always returns AppointmentAlreadyCancelledError (never succeeds); confirm() on a COMPLETED appointment always returns AppointmentAlreadyCompletedError; confirm() on CONFIRMED appointment returns AppointmentInvalidStatusTransitionError; cancel() is always idempotent: calling cancel() twice yields the same cancelledAt (within a single entity lifecycle); conflictsWith(self) always returns false; conflictsWith(other) is symmetric: a.conflictsWith(b) === b.conflictsWith(a); effectiveTimeSlot always contains timeSlot (effectiveStart <= startTime, effectiveEnd >= endTime) | Medium   | booking-scheduling          |
| OutboxPoller retry/DLQ accounting                   | apps/workers/events-worker/src/outbox/pollOutbox.ts                                                          | after maxRetries failures, event ends in dead_letter status, never PENDING; scheduleRetry backoff[i] is strictly monotonically increasing for i in [0, maxRetries-1]; retryCount on event after k failures equals k                                                                                                                                                                                                                                                                                                                                                                                                              | Medium   | webhooks-outbox-idempotency |
| stripeVerify timestamp window                       | packages/webhooks/src/framework.ts                                                                           | for any timestamp older than 300 seconds, stripeVerify always returns false regardless of signature validity; for any timestamp within 300 seconds with valid signature, stripeVerify always returns true; stripeVerify is constant-time (timingSafeEqual) — no timing oracle across varying signature lengths                                                                                                                                                                                                                                                                                                                   | Medium   | webhooks-outbox-idempotency |
| RetryBudgetTracker                                  | packages/platform/src/queues/retry-strategy.ts                                                               | After N calls to consumeRetry where N > defaultBudget, canRetry() must return false; After the windowMs elapses, budget resets to defaultBudget regardless of prior consumption; canRetry() is always consistent with the most recent consumeRetry() call in the same tick                                                                                                                                                                                                                                                                                                                                                       | Medium   | workers-queue-jobs          |
| ZepEpisodeUsage reset audit trail                   | apps/api/src/modules/zep/zep-budget.router.ts                                                                | For any sequence of reset operations, each audit entry's previousCount equals the episodesUsed that was in the DB immediately before that reset was applied; Consecutive audit entries form a consistent chain: entry[n].newCount === entry[n+1].previousCount                                                                                                                                                                                                                                                                                                                                                                   | Medium   | quota-budget-capacity       |
| AuditLogger async buffer                            | apps/api/src/security/audit/audit-logger.ts                                                                  | After flush() completes, the number of DB rows created equals the number of entries that were in the buffer at flush start; No entry appears more than once across all flush invocations for the lifetime of a single AuditLogger instance; flush() is idempotent when called concurrently: concurrent calls produce the same total DB row count as a single call                                                                                                                                                                                                                                                                | Medium   | audit-log                   |
| PrismaOutboxRepository.fetchPendingEvents           | packages/adapters/src/repositories/PrismaOutboxRepository.ts                                                 | The union of event IDs returned by two concurrent fetchPendingEvents calls on different connections is disjoint (no overlap); Every event ID returned by fetchPendingEvents that is subsequently markAsPublished never appears in future fetchPendingEvents results                                                                                                                                                                                                                                                                                                                                                              | Medium   | audit-log                   |
| TaxRate                                             | packages/domain/src/crm/billing/TaxRate.ts                                                                   | TaxRate(0).calculate(any) === Money.zero; TaxRate(r).calculate(Money.fromCents(n,c)).cents === Math.round(n \* r/100); calculate never returns negative amount; calculate result currency matches subtotal currency                                                                                                                                                                                                                                                                                                                                                                                                              | Medium   | pure-domain                 |
| Invoice state machine (transition exhaustiveness)   | packages/domain/src/crm/billing/billing-constants.ts                                                         | canTransitionInvoiceTo(s,s) === false for all statuses (no self-loops); isTerminalInvoiceStatus(s) iff VALID_INVOICE_TRANSITIONS[s].length === 0; all reachable statuses from DRAFT eventually reach a terminal state (liveness)                                                                                                                                                                                                                                                                                                                                                                                                 | Medium   | pure-domain                 |
| TimeSlot value object                               | packages/domain/src/legal/appointments/Appointment.ts                                                        | overlaps is commutative: a.overlaps(b) === b.overlaps(a); A slot does not overlap itself in a way that produces a non-zero conflict with identical boundaries unless they are the same entity; durationMinutes is always positive for any valid TimeSlot                                                                                                                                                                                                                                                                                                                                                                         | Low      | booking-scheduling          |
| LeadRoutingService.evaluateConditions               | apps/api/src/services/LeadRoutingService.ts                                                                  | For any LeadContext and any sequence of conditions, evaluateConditions returns false if any single condition fails (short-circuit correctness); The 'in' operator with an empty conditionValue array always returns false for scalar fields; The 'contains' operator on a numeric field always throws (never silently returns true/false); Operator evaluation is deterministic: same LeadContext + same conditions always yields the same boolean                                                                                                                                                                               | Low      | routing-assignment          |
| LeadRoutingService.computeRoutingScore              | apps/api/src/services/LeadRoutingService.ts                                                                  | Score is always in [0, 1] for any valid EligibleAgent with maxCapacity > 0; Score is 0 when maxCapacity is 0 (division guard); Agent with lower currentLoad always scores >= agent with higher currentLoad, given equal proficiency                                                                                                                                                                                                                                                                                                                                                                                              | Low      | routing-assignment          |
| evaluateDuplicateRules                              | apps/api/src/shared/duplicate-rule-evaluator.ts                                                              | score of every DuplicateMatch is in [0, 100]; no DuplicateMatch.candidate.id equals the input id when input.id is set; result is deterministic: same (input, candidates, rules) always produces identical output; empty candidates array always returns empty matches regardless of rules; inactive rules (isActive=false) never contribute matches; threshold=100 exact/normalized strategy only returns score=100 matches                                                                                                                                                                                                      | Low      | dedupe-merge                |
| extractDomainFromWebsite                            | apps/api/src/modules/account/account-duplicate-detection.service.ts                                          | null/undefined input always returns null; output never contains 'http://', 'https://', or 'www.'; output always contains a dot when input is a valid URL with a TLD; output is lowercase and trimmed                                                                                                                                                                                                                                                                                                                                                                                                                             | Low      | dedupe-merge                |
| calculateBackoffDelay                               | packages/platform/src/queues/retry-strategy.ts                                                               | For exponential type, delay(n) >= delay(n-1) for all n >= 1; calculatedDelay is always <= maxDelay when maxDelay is provided; With jitter=0, output is deterministic given the same inputs                                                                                                                                                                                                                                                                                                                                                                                                                                       | Low      | workers-queue-jobs          |
| RBACService.can                                     | apps/api/src/security/rbac.ts                                                                                | For any (userId, userRole, resourceType, action) without a user override, can() is pure and deterministic — same inputs always yield same output; OWNERSHIP_RESTRICTIONS[SALES_REP]=true implies can() with resourceOwnerId != userId always returns granted=false for write actions; getRoleLevel(ADMIN) > getRoleLevel(MANAGER) > getRoleLevel(SALES_REP) > getRoleLevel(USER) > getRoleLevel(VIEWER) for all valid roles; isRoleAtLevel(role, ROLE_LEVELS[role]) is always true for all named roles; DEFAULT_PERMISSIONS matrix is exhaustive: every (role, resource) pair has a non-null actions array                       | Low      | rbac-auth-session           |
| MfaService.verifyBackupCode                         | apps/api/src/services/mfa.service.ts                                                                         | verifyBackupCode is pure: same code + same hashedCodes always returns the same valid boolean; If valid=true, updatedCodes has exactly one fewer element than hashedCodes; After a code is consumed (removed from updatedCodes), verifyBackupCode with the same code against updatedCodes returns valid=false; verifyBackupCode never returns valid=true for a code not present in hashedCodes                                                                                                                                                                                                                                    | Low      | rbac-auth-session           |
| SessionService.isSessionValid                       | apps/api/src/services/session.service.ts                                                                     | isSessionValid is pure: same SessionData + same clock value always returns the same boolean; If expiresAt < now, isSessionValid returns false regardless of lastActiveAt; If now - lastActiveAt > inactivityTimeoutMs, isSessionValid returns false regardless of expiresAt                                                                                                                                                                                                                                                                                                                                                      | Low      | rbac-auth-session           |
| QueryBudgetStore.reported flag                      | packages/db/src/query-budget/extension.ts                                                                    | For any sequence of instrumentOperation calls on a single store, reportOverBudget is called at most once; store.reported is true after the first over-budget operation and remains true for all subsequent operations                                                                                                                                                                                                                                                                                                                                                                                                            | Low      | quota-budget-capacity       |
| ModuleRegistry.getMinimumPlanForModule              | packages/domain/src/platform/modules/ModuleRegistry.ts                                                       | For every ModuleId in CRM_MODULES, getMinimumPlanForModule returns a PlanTier such that MODULE_PLAN_MAP[tier] includes the module; CORE_CRM always returns undefined (no minimum plan); The returned tier is the lowest index in PLAN_TIERS that includes the module; isModuleInPlan(module, getMinimumPlanForModule(module)!) is always true for non-CORE modules                                                                                                                                                                                                                                                               | Low      | entitlement-modules         |
| ModuleRegistry.getModulesForPlan                    | packages/domain/src/platform/modules/ModuleRegistry.ts                                                       | CORE_CRM is always included in the result for every PlanTier; Plan tier ordering is monotone: if module M is in plan P, it is also in every plan tier with higher index than P in PLAN_TIERS; getModulesForPlan(plan).length >= 1 for all valid PlanTier values                                                                                                                                                                                                                                                                                                                                                                  | Low      | entitlement-modules         |
| Percentage                                          | packages/domain/src/shared/Percentage.ts                                                                     | create(v).value is rounded to exactly 2 decimal places; fromDecimal(d).asDecimal is approximately d (within 1e-10); add then subtract is approximately identity (up to clamping); of(100) === value for all valid Percentage                                                                                                                                                                                                                                                                                                                                                                                                     | Low      | pure-domain                 |
| LeadScore                                           | packages/domain/src/crm/lead/LeadScore.ts                                                                    | create(v,c).value === Math.round(v) for all v in [0,100]; tier is HOT iff value>=80, WARM iff value in [50,80), COLD otherwise; isHighConfidence iff confidence >= 0.8                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Low      | pure-domain                 |
| TimeSlot                                            | packages/domain/src/legal/appointments/TimeSlot.ts                                                           | overlaps is symmetric: a.overlaps(b) === b.overlaps(a); overlaps is reflexive: a.overlaps(a) === true; non-overlapping: a.gapMinutes(b) >= 0 iff !a.overlaps(b); durationMinutes === (endTime - startTime) / 60000 rounded; withBuffer(0,0) produces slot equal to original                                                                                                                                                                                                                                                                                                                                                      | Low      | pure-domain                 |
| DateRange                                           | packages/domain/src/shared/DateRange.ts                                                                      | overlaps is symmetric; isBefore(b) implies !overlaps(b); contains(start) === true for any valid range; durationInDays >= 0 always                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Low      | pure-domain                 |
| Email                                               | packages/domain/src/crm/lead/Email.ts                                                                        | create is idempotent: create(create(x).value).value === create(x).value (normalization is stable); domain() returns part after @; localPart() returns part before @; length <= 320 for all accepted emails                                                                                                                                                                                                                                                                                                                                                                                                                       | Low      | pure-domain                 |
| PhoneNumber                                         | packages/domain/src/shared/PhoneNumber.ts                                                                    | create normalises to E.164: result matches /^+[1-9]\d{1,14}$/; create is idempotent: create(create(x).value).value === create(x).value; countryCode + nationalNumber re-join to original value                                                                                                                                                                                                                                                                                                                                                                                                                                   | Low      | pure-domain                 |
| Ticket state machine (transition exhaustiveness)    | packages/domain/src/support/TicketConstants.ts                                                               | canTransitionTicketTo(s,s) === false for all statuses; isTerminalStatus(s) iff transitions empty; ARCHIVED is only reachable from RESOLVED or CLOSED — no shortcut from OPEN                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Low      | pure-domain                 |
| Lead domain aggregate                               | C:/Users/talys/projects/intelliFlow-CRM/packages/domain/src/crm/lead/Lead.ts                                 | Lead.create always produces status='NEW' regardless of input; convert() called twice always returns LeadAlreadyConvertedError on second call; qualify() only succeeds from NEW or CONTACTED status — forall status in LEAD_STATUSES minus those two, qualify() returns failure; LeadScore value is always in [0,100] after updateScore() with any valid confidence; Email value object rejects any string not matching RFC 5321 local@domain pattern                                                                                                                                                                             | Low      | test-infra                  |
| Opportunity domain aggregate                        | C:/Users/talys/projects/intelliFlow-CRM/packages/domain/src/crm/opportunity/Opportunity.ts                   | markAsWon() followed by markAsLost() always throws OpportunityAlreadyClosedError; DEFAULT_STAGE_PROBABILITIES maps all 7 stages to values in [0,100]; updateValue() with negative amount always returns InvalidOpportunityValueError                                                                                                                                                                                                                                                                                                                                                                                             | Low      | test-infra                  |
| PrismaOutboxRepository.fetchPendingEvents           | C:/Users/talys/projects/intelliFlow-CRM/packages/adapters/src/repositories/PrismaOutboxRepository.ts         | Two concurrent pollers never return the same event ID (FOR UPDATE SKIP LOCKED correctness); fetchPendingEvents with limit=N returns at most N rows                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Low      | test-infra                  |
| PrismaLeadConversionRepository idempotency          | C:/Users/talys/projects/intelliFlow-CRM/packages/adapters/src/repositories/PrismaLeadConversionRepository.ts | save() with an existing idempotencyKey always throws (@@unique([idempotencyKey]) at schema line 4843); findByLeadId with same (leadId, tenantId) always returns the same record after one save                                                                                                                                                                                                                                                                                                                                                                                                                                   | Low      | test-infra                  |

---

_107 invariants + 43 property candidates. Regenerate from
`race-condition-findings.json`._
