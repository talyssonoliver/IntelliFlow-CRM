Codex Semantic Review — 2026-06-12T23:52:14.386Z
Base ref  : origin/main
Files in scope : 8
Total findings : 2
  Waived       : 0
  Unwaived     : 2

UNWAIVED FINDINGS (BLOCKING):
  [MEDIUM] apps/web/src/app/contacts/[id]/page.tsx:2411 — broken-create-deal-prefill
    Issue    : The Add Deal action navigates with a contactId query parameter that the target create-deal page never reads, so the new deal is not pre-associated with the contact.
    Evidence : Changed code calls `router.push(buildDealNewHref(contactId))`, which produces `/deals/new?contactId=...`; however `apps/web/src/app/deals/(list)/new/page.tsx` renders `<DealForm mode="create" ... />` without `useSearchParams` or `initialData`, so `DealForm` keeps its default `contactId: ''`. The new test only asserts the URL, encoding the same false contract assumption.
    Fix      : Update the new-deal page to read `contactId` from search params and pass it through `DealForm initialData` with the required account context, or change this action to a route/API flow that actually creates or preloads a contact-linked deal.
    Fingerprint: 51e522fb5159252f3a82d4bc0c3bc2c2c176f40139841e0e81ab2c8cbab8f933

  [MEDIUM] apps/web/src/components/contacts/ContactQuickActions.tsx:70-76 — invalid-log-call-clears-input
    Issue    : Log Call submits unchecked title/description lengths and immediately clears the dialog, so backend validation failures lose the user's call notes without logging the activity.
    Evidence : `onLogCall({ contactId: contact.id, type: 'CALL', title, description: logCallDescription.trim() || undefined }); resetLogCall();` sends the payload and clears state unconditionally. The real `logActivitySchema` requires `title` max 200 and `description` max 2000, but this component has no matching maxLength or validation, and the page wires `onLogCall` to fire-and-forget `logActivityMutation.mutate(input)`, so over-limit input is rejected after the dialog has been cleared.
    Fix      : Mirror the backend max-length validation in the dialog and only reset after a confirmed successful mutation, for example by making `onLogCall` return a promise via `mutateAsync` or by closing from the mutation success callback.
    Fingerprint: 00dad401c81f351b491e8ff6d5e159c9c7485742e73edb84753dd3102503e007

Verdict: FAIL