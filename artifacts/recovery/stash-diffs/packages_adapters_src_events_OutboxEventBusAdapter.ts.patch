diff --git a/packages/adapters/src/events/OutboxEventBusAdapter.ts b/packages/adapters/src/events/OutboxEventBusAdapter.ts
index a75a92a..9e52cef 100644
--- a/packages/adapters/src/events/OutboxEventBusAdapter.ts
+++ b/packages/adapters/src/events/OutboxEventBusAdapter.ts
@@ -10,6 +10,7 @@
  */
 
 import { PrismaClient, EventStatus, Prisma } from '@prisma/client';
+import { withTransaction } from '@intelliflow/db';
 import { DomainEvent } from '@intelliflow/domain';
 import { EventBusPort } from '@intelliflow/application';
 
@@ -111,7 +112,7 @@ export class OutboxEventBusAdapter implements EventBusPort {
       return;
     }
 
-    await this.prisma.$transaction(async (tx) => {
+    await withTransaction(async (tx) => {
       for (const event of events) {
         const idempotencyKey = this.generateIdempotencyKey(event);
 
