/**
 * Test stub for @intelliflow/platform/queues/types.
 *
 * The platform package has no built dist in dev/CI test environments.
 * Exports the minimal symbols used by worker-shared source files.
 */

export const DEFAULT_QUEUE_CONFIGS: Record<string, unknown> = {};
