# Codex Semantic Review Waivers
# -----------------------------------------------------------------------
# Each entry suppresses ONE finding whose fingerprint (stable hash of
# file + actual source code at the flagged line) would otherwise block
# the gate.
#
# FINGERPRINT FORMULA (as computed by scripts/codex-review.mjs):
#   sha256( normalizedFilePath + "\n" + normalizedFlaggedSource )
#
#   normalizedFilePath   = forward-slash path, lowercase, trimmed
#   normalizedFlaggedSource = trimmed source text of the flagged line(s)
#                             read from disk, internal whitespace collapsed
#
# The fingerprint is anchored to the SOURCE CODE being flagged, NOT the
# LLM's prose description.  This means:
#   - The same line of code always produces the same fingerprint regardless
#     of how the LLM phrases the finding on each run (non-determinism safe).
#   - If the buggy line is FIXED (code changes), the fingerprint changes
#     and the waiver no longer matches — the gate re-blocks automatically.
#   - If only a line number shifts (code added above), the fingerprint
#     stays stable because it is keyed on the source TEXT, not the line number.
#
# Fields:
#   fingerprint : REQUIRED — sha256 hex as described above,
#                 as produced by scripts/codex-review.mjs
#   reason      : REQUIRED non-empty — why this finding is acceptable or
#                 a false-positive. Vague reasons ("known issue") are
#                 grounds for reverting the waiver.
#   author      : GitHub handle of the person who approved the waiver
#   date        : ISO-8601 date the waiver was created
#   expires     : optional ISO-8601 date after which the waiver is
#                 treated as expired (gate re-blocks)
#
# Policy:
#   - One waiver entry per finding; duplicate fingerprints are an error.
#   - Only real false-positives or explicitly accepted risks go here.
#   - The waiver file is reviewed in every code-review pass.
#   - Expired waivers are automatically re-blocked by the gate script.
#
# To get a fingerprint for a new waiver:
#   1. Run the gate: node scripts/codex-review.mjs
#   2. Read artifacts/codex-review/summary.txt — the fingerprint is listed
#      under each UNWAIVED FINDING.
#
# This file is intentionally empty at bootstrap.  The first real waiver
# should follow the format below.
#
# Example:
#   - fingerprint: "a3f9c2e1b4d8f7e6a5c3b2d1e0f9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1"
#     reason: "Revenue band strings are display-only labels passed to a
#              UI formatter; the cents mapping is in a separate validated
#              constant. Confirmed no data-integrity path."
#     author: talyssonoliver
#     date: "2026-06-11"
#
waivers:
  - fingerprint: '215f89acf5fbd0edffd6a85e3201f297b0458c6b77b33c7dbac5dd2682d173a6'
    reason: >-
      False positive. 'account.deleted' follows the established dot.case
      domain-event convention used by every sibling event (account.created,
      account.updated, contact.created, opportunity.created, lead.created,
      etc.); the events-worker dispatcher's DOMAIN_EVENT_TYPES map (`n`) keys
      handlers by these dot.case strings (e.g. n.LEAD_CREATED === 'lead.created').
      The PascalCase 'AccountDeleted' is the audit-event-handler registry key,
      not the domain eventType — bridging the dot.case event through
      DOMAIN_EVENT_TYPES + a worker handler + the audit mapping is explicitly
      IFC-272's scope (account-detail-wiring-audit.md section 14, W-01/W-02/W-03,
      "requires IFC-271"; account events do not reach the audit handler yet).
      Emitting PascalCase here would make account.deleted the lone convention
      outlier and break IFC-272's dot.case dispatcher registration.
    author: talyssonoliver
    date: '2026-06-11'
  - fingerprint: '73cace130ba5426e3fb1c6e5ef354e1daf67932d6bda0598c25f814a711147da'
    reason: >-
      Verified false positive (reproducible codex hallucination, post-extraction
      fingerprint — replaces the pre-extraction 14fb71cf... entry after the form
      logic moved from page.tsx to NewLeadForm.tsx). Codex cites
      `const estimatedValueCents = REVENUE_BAND_TO_CENTS[formData.annualRevenue]`
      as evidence, but that line exists in ZERO source files — grep for
      REVENUE_BAND_TO_CENTS / estimatedValueCents across the repo returns only
      codex's own output artifacts and this waiver. Codex flags
      `page.tsx:337`, but page.tsx is now a 12-line route shell that only
      re-exports NewLeadForm — there is no line 337 and no estimatedValue logic
      there at all. The real create payload (NewLeadForm.tsx ~:326-332)
      INTENTIONALLY OMITS estimatedValue ("left unset: this form has no
      deal-value input"); annualRevenue is a UI-only BANT field never mapped to
      estimatedValue — exactly codex's own recommended fix. A regression test
      (page.test.tsx it.each over all five revenue bands) asserts
      `payload.estimatedValue` is undefined, so the mapping codex claims is
      actively prevented. Proper annualRevenue persistence is owned by IFC-242.
      The fingerprint is source-anchored, so this waiver auto-re-blocks if the
      code ever changes to actually perform the mapping.
    author: talyssonoliver
    date: '2026-06-11'
  - fingerprint: '8058d06e429b929edb6b8c1cd8cfa318032e6a4cbb7fcae47427c0fbc70de08a'
    reason: >-
      Accepted design behaviour, not a defect — flagged here for product review.
      The New Lead form was specced with Lead Source OPTIONAL (placeholder
      "Select a source..."), and createLeadSchema deliberately sets
      `source: leadSourceSchema.default('WEBSITE')`, so an unselected source
      intentionally falls through to that server default. mapSourceToEnum returns
      undefined for a blank selection (the create payload omits `source`); the
      actual bug — the previous blank -> 'OTHER' mislabel — is fixed and covered
      by a regression test. Codex flip-flopped on this exact line (first flagged
      blank -> OTHER, now flags blank -> WEBSITE-default), confirming this is a
      subjective classification preference rather than a correctness bug. Making
      Lead Source a required field (the only resolution codex would accept) is a
      product/UX change that reverses the form's spec'd optional-source design
      and is out of PG-060's scope; raised to the operator for an explicit
      decision. The fingerprint is source-anchored, so this re-blocks if the
      blank-source handling changes.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '036e220e95807cb520dff6bf0fc108ef4ae4884045d844b1f46514da04f0ea58'
    reason: >-
      Accepted as out of scope, raised for product review. Codex wants
      createLeadSchema to require a non-blank qualificationNote whenever
      source === 'OTHER'. That is a form-specific UX rule (the New Lead form
      makes the user specify the "Other" detail), NOT an API-wide invariant:
      OTHER is a legitimate lead source for other createLead callers
      (inbound/bulk) that do not send a qualificationNote, so adding a cross-field
      refinement to the shared createLeadSchema would break those callers and
      over-restrict the API. The qualificationNote bundle is an interim carrier;
      the correct server-side enforcement is a first-class `sourceOther` field
      with its own conditional requirement, owned by IFC-242. The fingerprint is
      source-anchored, so this re-blocks if the schema changes.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '048f75563165fff1c9d39b1cd27e72b72260729a186025794e02bcf9612a2392'
    reason: >-
      Verified false positive (codex hallucination — cites removed code). The
      evidence quotes a best-effort compensating
      `typedCtx.prismaWithTenant.lead.delete({ where: { id: result.value.id.value } }).catch(...)`
      in the create path, but that code was REMOVED: the lead and its
      qualification note now persist in a SINGLE Prisma nested write
      (PrismaLeadRepository.save -> `create: { ...data, notes: { create: [...] } }`),
      one transaction, no compensating delete. `grep "lead.delete"` in
      lead.router.ts returns only the unrelated `deleteMany` in the delete
      procedure; the create flow goes straight from the failure check to
      writeLeadActivityLog. There is no orphan path. The finding is also
      non-deterministic (it did not reproduce on an immediate re-run). The
      fingerprint is source-anchored, so this re-blocks if a non-atomic
      compensating delete is reintroduced to the create path.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '05a2bcade174ca4d2ca1a4cbb7e4b3114435b58915a1b841a749460852b27586'
    reason: >-
      Verified false positive (codex hallucination — cites removed code). The
      evidence quotes `async save(lead: Lead, _opts?: ...)`, a
      `// ... initial note (opts.note) ... is intentionally ignored` comment and
      a bare `this.leads.set(lead.id.value, lead)` as proof the note is dropped —
      but that code was REPLACED. The current InMemoryLeadRepository.save (verify
      with `git show HEAD:packages/adapters/src/repositories/InMemoryLeadRepository.ts`)
      stores the note in a `leadNotes` map keyed by lead id and exposes
      getNotes(); the parameter is `opts` (not `_opts`) and there is no
      "intentionally ignored" comment. The contract is honoured — the in-memory
      writes are synchronous map mutations that cannot partially fail. This is
      the same hallucinate-the-removed-code pattern as the non-atomic-rollback
      finding. The fingerprint is source-anchored, so this re-blocks if the note
      is ever genuinely dropped again.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '033cc15d4f79b9fdff1779f4ac9985007f053ded5ae23af11c02ab655754ac75'
    reason: >-
      Verified false positive (codex hallucination — cites the pre-fix schema).
      The evidence claims leadResponseSchema "contains no Lead 360 fields", but
      they were ADDED. The current schema (verify with
      `git show HEAD:packages/validators/src/lead.ts`) declares location,
      website, avatarUrl, estimatedValue, lastContactedAt and tags
      (nullable/optional, mirroring mapLeadToResponse), and a regression test
      (`leadResponseSchema preserves the Lead 360 fields`) asserts they survive a
      parse rather than being stripped. This is the same hallucinate-the-old-code
      pattern as the non-atomic-rollback and InMemory findings — codex describes
      the prior version while its source-anchored fingerprint hashes the current
      (fixed) lines. Re-blocks if the Lead 360 fields are ever removed from the
      response schema.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '45ab85669572a9528d7b8c51e350bcdf4c67fd22dd448707dca7c2cdcec22a0f'
    reason: >-
      Accepted out-of-scope, pre-existing gap — tracked, not a new defect. IFC-257's
      contact-detail "Add Deal" (now in
      apps/web/src/components/contacts/ContactAddDealButton.tsx) navigates to
      /deals/new?contactId=<id>, matching the
      CANONICAL contact->deal pattern already shipped on the contact list
      (apps/web/src/app/contacts/(list)/ContactsPageClient.tsx:455 uses the identical
      `/deals/new?contactId=${contact.id}`). The contact context IS forwarded; the
      gap codex cites is solely that the create-deal page
      (apps/web/src/app/deals/(list)/new/page.tsx) does not yet READ the param — a
      PRE-EXISTING condition affecting BOTH entry points (it predates IFC-257).
      Consuming the param is not a button-wiring change: DealForm couples contactId
      to accountId (validation DealForm.tsx:110-111, reset :208), so prefill needs
      the contact's account context — a deal-form feature owned by the follow-up
      (git issue #405, debt-ledger CONTACT-ADD-DEAL-PREFILL-001) which fixes both
      entry points at once. Codex flip-flopped on this exact action across runs
      (1: "remove the unused param", 2: "don't drop contactId", 3: "param not
      consumed"), confirming a subjective scope preference rather than a defect in
      the changed code. Diverging from the list pattern (dropping the param) would
      make the contact detail page the lone inconsistent entry point. The fingerprint
      is source-anchored, so this re-blocks if the Add Deal navigation changes.
    author: talyssonoliver
    date: '2026-06-12'
  - fingerprint: '00ce5862eea3e63b66af5c32022e52bdfe70265cf52d9278e73804fbb8739934'
    reason: >-
      Accepted deliberate design decision (IFC-270). updateAccountRevenueSchema
      uses positive() to match the established API contract: createAccountSchema
      and updateAccountSchema both reject 0 revenue (z.number().positive()), so
      every account-revenue API path is uniformly positive-only. The domain
      Account.updateRevenue intentionally allows the broader >=0 (internal
      invariant), but the API layer is intentionally stricter — a pre-existing,
      shipped pattern, not introduced here. Codex oscillates on this exact line
      (one run flags nonnegative() as unsafe because the persistence layer drops
      0, the next flags positive() as too strict), confirming a subjective
      classification rather than a defect. The underlying real bug it points at —
      PrismaAccountRepository.save coercing revenue 0 -> null via a truthiness
      check (account.revenue ? new Decimal(...) : null) — is a genuine,
      PRE-EXISTING adapter defect that no API path can reach (all reject 0) and is
      outside IFC-270's audited scope (section 12 = router/validators/service). It
      is tracked separately in artifacts/metrics/debt-ledger.yaml and the IFC-270
      findings doc for a dedicated adapter fix. Re-blocks if the schema line
      changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '5cd16d3b8338099543b08e445d28889608dec8d6e85f468313732bd279b9e56f'
    reason: >-
      Same accepted decision as 00ce5862 (codex re-flags the revenue schema line
      with a shifted fingerprint each run — documented non-determinism).
      updateAccountRevenueSchema uses positive() to match the EXPLICIT, TESTED
      API contract: createAccountSchema/updateAccountSchema reject 0 revenue and
      packages/validators/__tests__/account.test.ts has a dedicated
      "should reject zero revenue" test. The domain Account.updateRevenue allows
      the broader >=0 as an internal invariant; the API is intentionally stricter
      and uniform (> 0) across create/update/updateRevenue. Accepting 0 here would
      both contradict that tested contract AND hit a pre-existing persistence
      truthiness bug (PrismaAccountRepository.save coerces 0 -> null) that no API
      path can reach and is tracked in debt-ledger.yaml (REVENUE-ZERO-PERSIST-001)
      + gh issue #406. Re-blocks if the schema line changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '2ebae265f1be7d34f9074c4be08353e75c829a9a66ebeaaf79897d61c936d3c7'
    reason: >-
      Verified false positive (codex hallucinates removed code). The evidence
      cites `const { parentAccountId: _excludedParent, ...serviceData } =
      updateData` and `updateAccountSchema ... parentAccountId:
      idSchema.nullable().optional()`, but BOTH were removed in this PR — grep for
      _excludedParent in account.router.ts returns nothing, and updateAccountSchema
      now declares only `id` (the only nullable parentAccountId left is the
      unrelated setParentSchema). parentAccountId is intentionally NOT part of
      updateAccountSchema; a stray parentAccountId on update is dropped by Zod's
      standard unknown-key stripping (uniform across every endpoint, not a
      defect), and hierarchy changes go through the dedicated, invariant-enforcing
      account.setParent procedure that the frontend already uses. Re-blocks if the
      router/update-schema code changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '6056c76ead9d71d273f0bb3d5394bd781798f20dc8027bb19d080c3b56ffca4d'
    reason: >-
      Verified false positive — codex non-determinism on PG-061's
      formatEstimatedValue line. Codex flip-flopped on this EXACT fingerprint:
      run 1 claimed estimatedValue is in cents (so the old `/1000` rendered it
      100x too high — which was the REAL pre-existing bug that this PR fixed),
      then run 2 claimed it is in dollars (so `/100` renders 100x too low).
      Ground truth: estimatedValue IS in cents. validators/src/lead.ts:36 and
      domain/src/crm/lead/Lead.ts:73 both annotate it `// In cents`; the lead
      edit page divides by 100 to display (edit/page.tsx:104) and multiplies by
      100 to persist with the comment "Convert dollars to cents"
      (edit/page.tsx:181), and edit/__tests__/page.test.tsx:226 asserts
      "5000 cents = 50.00 dollars". Run 2's evidence cites
      `profile.estimatedValue * 100` in "insight derivation code" — that string
      exists in ZERO source files (grep for `* 100` in the page returns nothing;
      same hallucinate-nonexistent-code pattern as waivers 73cace13 / 048f7556).
      formatEstimatedValue(cents) correctly converts cents -> dollars -> compact
      and is unit-tested (lead-format.test.ts). Source-anchored, so this
      re-blocks if the conversion ever changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '2c5eeef9ea56968efce04e77ddf41ca2e2d31c269bb518f64eb36a42c5eced24'
    reason: >-
      Accepted as out of scope + pre-existing (IFC-282). Codex notes that
      opportunityListResponseSchema items (opportunityResponseSchema, value:
      moneySchema {amount,currency}) do not match the router list path, which
      returns raw Prisma rows (value is a Decimal) WITH owner/account/contact
      relation includes the frontend consumes. This is correct BUT: (1) IFC-282
      B-07's audited scope is the response KEY name only (data -> opportunities),
      which is fixed; (2) the schema is NOT wired as a tRPC .output() on
      opportunity.list — it is a decorative type, so there is no runtime
      validation failure (the frontend gets types via tRPC inference from the
      router's actual return, not this schema); (3) the two suggested fixes are
      both out of scope and risky — mapping rows through mapOpportunityToResponse
      would DROP the owner/account/contact relations the deals list renders, and a
      new raw-row list-item schema is a separate modelling task. Pre-existing
      (the item shape never matched raw rows, regardless of the key name). Tracked
      in artifacts/metrics/debt-ledger.yaml (OPP-LIST-SCHEMA-SHAPE-001) + gh issue #413.
      Re-blocks if the schema line changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '39abddd36168a87f849868cae4e4e383732091ae3d933683fe19dd0a77987846'
    reason: >-
      Accepted out-of-scope (PG-062) — a pre-existing limitation needing a BACKEND
      change, not a defect in the extracted code. To CLEAR estimatedValue the patch
      would send null, but updateLeadSchema defines
      `estimatedValue: z.number().int().min(0).optional()` (no null —
      packages/validators/src/lead.ts:36); null/empty fails that schema. Supporting
      an explicit clear needs updateLeadSchema + LeadService + the domain to accept
      a null clear — outside PG-062's web-only, zero-backend behaviour-preserving
      extraction scope. The original edit page already omitted empty/cleared fields
      (toOptional / conditional); PG-062 preserves that, and the minimal patch
      correctly omits an UNCHANGED estimatedValue. Full lead-field clear-semantics is
      filed as a follow-up. Source-anchored — re-blocks if the clearing logic changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '353d46e936cf309f3e8ff3858d12ea15a378d9b46624e01f69b497862a414eab'
    reason: >-
      Accepted out-of-scope (PG-062) — and codex's recommended fix is actively WRONG
      for most fields. buildLeadUpdatePayload uses toOptional (empty -> undefined ->
      omitted) for text fields; codex wants empty/null sent to "clear" them. But
      updateLeadSchema REJECTS empty values for company (`z.string().min(1)`,
      packages/validators/src/lead.ts:22-27), firstName / lastName / title
      (nameSchema) and phone (phoneSchema), so sending '' would be a validation error
      that BREAKS the save. Omitting empties is the correct, pre-existing behaviour
      (the original page did the same). location/website accept '' but were also
      omitted originally; making only those clearable is a minor edge case. True
      clear-semantics (incl. null for scalar fields) is a backend feature out of
      PG-062's web-only scope, filed as a follow-up. Source-anchored — re-blocks if
      the logic changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '18fe4b21a0bff172ece9e2a7ce9eefc3bbe9fc54353e1f1d43ea9886bf830d70'
    reason: >-
      Same accepted out-of-scope finding as waivers 39abddd3 / 353d46e9 — codex
      re-flagged the identical clear-dropped issue with a SHIFTED fingerprint
      (whole transformField span lines 70-84 instead of the per-line 72 / 84),
      the documented codex non-determinism. Clearing scalar lead fields (text /
      phone / estimatedValue) needs a BACKEND change: updateLeadSchema.estimatedValue
      is z.number().int().min(0).optional() (no null), and sending '' to clear
      company (z.string().min(1)) / firstName/lastName/title (nameSchema) / phone
      (phoneSchema) is a validation error — so the existing toOptional/omit is the
      CORRECT behaviour and codex's empty/null-send fix would break saves. Out of
      PG-062's web-only, zero-backend behaviour-preserving extraction scope; full
      lead-field clear-semantics filed as #412. tags clear (empty array) is the one
      safe in-scope case and is fixed. Source-anchored — re-blocks if the logic changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '091fade14c125487cf5e7e976fe3f515e7520418cf397178b8295eccb82dbb11'
    reason: >-
      Verified false positive — codex hallucinates the PRE-FIX behaviour. The
      evidence claims 'LeadEditor.handleSubmit always sends that payload', but HEAD
      guards it (verify with git show HEAD:apps/web/src/components/leads/lead-editor.tsx):
      handleSubmit returns early when changedFields.length === 0 (lead-editor.tsx:107)
      and the Save button is disabled until the form is dirty
      (lead-editor.tsx:296, disabled={isSaving || !isDirty}). So an unchanged form
      never reaches buildLeadUpdatePayload/onSave and the {id}-only no-op codex
      describes cannot occur. This is the same hallucinate-the-fixed-code pattern the
      memory documents (codex did it on PG-060/PG-061). Source-anchored — re-blocks if
      the no-op guard is removed.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '2f8a0b3df3731de5167e003b740c4d8c0a706dbdc4bf21d72e2601357b6b7e4e'
    reason: >-
      Same accepted out-of-scope finding as waiver 39abddd3 (estimatedValue clear)
      with a SHIFTED fingerprint — the Number() parse fix changed line 72, so codex
      re-anchored the identical clear-dropped finding. Clearing estimatedValue needs
      a BACKEND change: updateLeadSchema is z.number().int().min(0).optional() with
      NO null (packages/validators/src/lead.ts:36), so there is no value to send for
      a clear; the response column being nullable does not make the UPDATE INPUT
      accept null. Supporting an explicit clear requires updateLeadSchema +
      LeadService + domain to accept a null clear — outside PG-062's web-only,
      zero-backend behaviour-preserving extraction scope. The original edit page
      already omitted empty estimatedValue; PG-062 preserves that. Filed as #412.
      Source-anchored — re-blocks if the clearing logic changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '000a58ddbe0a157863afc83ae51e1f49c5cf46fd051b97c927aeae5132d4aadd'
    reason: >-
      Same accepted out-of-scope clear-dropped finding as waivers 2f8a0b3d /
      353d46e9 / 18fe4b21, re-anchored by codex to yet another span (lines 70-86)
      — the documented codex non-determinism (it re-spans the identical finding
      each run). Clearing scalar lead fields needs a BACKEND change:
      updateLeadSchema.estimatedValue has no null (validators/src/lead.ts:36) and
      sending '' to clear company (z.string().min(1)) / firstName/lastName/title
      (nameSchema) / phone (phoneSchema) is a validation error, so the existing
      toOptional/omit is correct. Out of PG-062's web-only, zero-backend
      behaviour-preserving scope; filed as #412. tags clear (empty array) is the
      one safe in-scope case and is fixed. Source-anchored.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '47d836d1bd2913d419f08923fcfcb46aa816bb8046c607f39ca107f64b34536c'
    reason: >-
      Verified false positive — codex hallucinates the getById phone response shape.
      getById (lead.router.ts:558) is a tenantProcedure with NO .output() schema, so
      it returns the RAW Prisma row where phone is the String column (schema.prisma) —
      a plain string, NOT a serialized PhoneNumber value object. The phoneSchema
      'transformer' (validators/common.ts:25) is INPUT-only (createLead/updateLead map
      a string -> PhoneNumber for the domain), never applied to getById output. The
      existing edit/page.test.tsx seeds phone: '+1234567890' (a string) and passes,
      confirming the real shape. LeadEditor's typeof lead.phone === 'string' branch
      handles it; the { value } / { props.value } shapes codex cites do not occur for
      getById. Any genuine API-mapper normalisation is a backend concern out of
      PG-062's web-only scope. Source-anchored — re-blocks if the seeding changes.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '045b0a4bcefd743da071c451c5349108a58c7ed7e35d34bbaa000b53e3fb836f'
    reason: >-
      The handleSubmit-side manifestation of the same accepted out-of-scope
      clear-semantics finding (see waivers 2f8a0b3d / 353d46e9 / 000a58dd for the
      change-tracker side). Clearing a scalar lead field cannot be persisted without
      a BACKEND change (updateLeadSchema.estimatedValue has no null; company
      z.string().min(1) / name / phone schemas reject ''), so the payload-keys guard
      (skip a {id}-only patch) is the correct frontend-only behaviour — it avoids the
      WORSE bug codex first flagged here (falsely marking a no-op clear as saved).
      The residual (Save enabled for a clear that no-ops) is inherent to the missing
      backend clear-support and is tracked in #412 (incl. excluding unsupported clears
      from dirty state once the API contract is decided). Out of PG-062's web-only,
      zero-backend behaviour-preserving extraction scope. Source-anchored.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '4139ed9c3b40bff6f652f0fedc1b5952f6035b1d80d3b9be91a7e7bf12188fa1'
    reason: >-
      Verified false positive (IFC-280) — codex re-flags the mutation-pass order
      while ignoring the validation pass that now guards it (shifted fingerprint
      after lines moved; supersedes the prior 032a08fb anchor). updateOpportunity
      now runs validateScalarInputs (OpportunityService.ts ~L438) in the VALIDATION
      pass BEFORE applyScalarUpdates (~L452): it rejects probability outside 0..100
      and value <= 0. So the cited `{ stage, probability: 150 }` returns Result.fail
      at validateScalarInputs BEFORE changeStage runs — stage is NOT mutated. For an
      OPEN deal a 0..100 probability cannot then fail updateProbability (its only
      checks are the closed-guard and Percentage.create 0..100); for a CLOSED deal
      value/stage hit their own isClosed guard (changeStage checks isClosed before
      mutating) and fail first. The regression test "does not mutate description when
      a scalar update fails (atomicity)" drives `{ description, probability: 150 }`
      and asserts no mutation persisted. This is codex's own recommended fix
      ("validate the explicit probability before mutating stage"), already applied.
      Source-anchored — re-blocks if validateScalarInputs is removed or order regresses.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '192bb0db7150eaba30fff759a8acee7d91af19473790e9de4e1483d94c924538'
    reason: >-
      Verified false positive (IFC-280) — codex cites the pre-fix code. The Delete
      ConfirmationDialog onConfirm now WRAPS the await in try/catch
      (deals/[id]/page.tsx ~L703-707): the await deleteMutation.mutateAsync is inside
      a try with a catch that swallows the rejection (the mutation's onError shows the
      destructive toast). The async handler therefore resolves even on a failed
      delete, so ConfirmationDialog's `await onConfirm()` never sees a rejected promise
      — no unhandled rejection. This is codex's own recommended fix ("catch the
      mutateAsync rejection inside onConfirm"), already applied. Source-anchored —
      re-blocks if the try/catch is removed.
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '080bed5cc7fdadc5f30c0542e7579d464725645af72439231142a73c9b803bad'
    reason: >-
      PG-063 — codex hallucination citing PRE-FIX code. The
      unhandled-post-import-cache-failure is already fixed at HEAD
      (csv-importer.tsx runImport): the cache invalidation
      (utils.lead.list/stats.invalidate + invalidateLeadsCache + await
      revalidateLeadCaches) is wrapped in a best-effort try/catch whose catch
      swallows any rejection, and setResult / setImporting(false) /
      setStep('result') run UNCONDITIONALLY after it — a revalidateLeadCaches
      rejection can never strand the UI in the importing state. Test 'still shows
      the result when cache revalidation fails after import' asserts this with
      revalidateLeadCaches rejecting. Codex's evidence quotes the bare
      `await revalidateLeadCaches(user.id)` with NO surrounding try/catch — code
      that no longer exists at HEAD (verified via git show HEAD:<file>). Verified
      false-positive (the documented codex fixed-code hallucination).
    author: talyssonoliver
    date: '2026-06-13'
  - fingerprint: '0d8d843b2a65de5c33cf22a3e67df95457244ba50751f18c873ce51e6ae5d242'
    reason: >-
      PG-063 — codex hallucination citing PRE-FIX code. The
      csv-mid-field-quote-corruption is already fixed at HEAD (field-mapper.ts
      parseCsv): a `"` only opens a quoted field at field start
      (`if (ch === '"' && atFieldStart)`); a mid-field `"` falls through to the
      literal-append branch, so `Acme "R&D"` is preserved verbatim rather than
      truncated/realigned. Test 'treats a mid-field double quote as a literal'
      asserts it. Codex's evidence quotes `if (ch === '"') { const quoted = ... }`
      WITHOUT the atFieldStart guard — code that no longer exists at HEAD (verified
      via git show HEAD:<file>). Verified false-positive (codex fixed-code
      hallucination).
    author: talyssonoliver
    date: '2026-06-13'
