/**
 * Seed IDs Re-export for Prisma Seeding
 *
 * This file re-exports from the canonical source in src/seed-ids.ts
 * The canonical source is in src/ so it can be built and exported from the package.
 *
 * For package consumers, use: import { SEED_IDS } from '@intelliflow/db/seed-ids'
 */

export { SEED_IDS } from '../src/seed-ids';
export type { SeedIds } from '../src/seed-ids';
